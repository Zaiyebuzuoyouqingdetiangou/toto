## 0.32.35 根容器自匹配隔离修复版

- 修复带完整 `<style>` 的兔子镜在折叠后重新展开、切换 swipe 或消息重新挂载后，只剩标题和空白区域的问题。
- 不再只凭 `<style>` 上的“已隔离”标记跳过处理；会核对当前实时 CSSStyleSheet，发现 CSSOM 已被 WebView 重建时自动重新建立局部作用域。
- 兼容酒馆把生成 class 改写成 `custom-xxx`、但重建后的样式仍保留原 class 选择器的情况；根容器及其子层会通过局部 `:is(.原名, .custom-原名)` 重新匹配。
- 已隔离的 `@keyframes` 不会被重复追加作用域后缀，避免每次扫描继续改名。
- 仅修复 0.32.34 的 CSS 隔离层；Prompt、展现形式优先配色、单镜输出锁、纯文字急救、代码块急救和交互急救均未修改。

## 0.32.34 兔子镜局部 CSS 隔离修复版

- 修复不同消息中的兔子镜重复使用 `.rabbit-mirror-container`、`.main-title`、`.answer` 等通用 class 时，后生成的 `<style>` 覆盖旧消息，导致只剩背景、内容消失或整套 UI 串样式的问题。
- 每个已渲染兔子镜会获得独立的消息内 CSS 作用域；其 `<style>` 选择器只对当前兔子镜生效，不再污染同一聊天中的其他作品。
- 同名 `@keyframes` 也按当前兔子镜隔离，避免多个作品都使用 `pulse`、`float` 等动画名时互相覆盖。
- 能在聊天载入、切换 swipe、更新消息后重新建立隔离，因此当前已经显示异常的旧消息也会重新恢复。
- 本次不修改 Prompt、展现形式优先配色、单镜输出锁、纯文字急救、代码块急救或交互急救逻辑。

## 0.32.33 单镜输出锁修复版

- 修复部分模型把开头“硬启动锁”和末尾“最终输出契约”分别执行，导致正文上下各生成一份相同兔子镜的问题。
- 开头改为“启动锚点”：只预留长度，明确不得在正文前输出、预演、复制或占位。
- 末尾契约明确全回复只能出现一个 `<toto>...</toto>`，且必须位于正文之后、作为最后一个可见模块。
- 保留 0.32.32 的展现形式优先配色、全部交互急救、纯文字急救误伤保护与 Visual Scenery 规则。
- 未新增输出清洗或自动删除逻辑；本次仅修正 Prompt，避免误伤正常内容。

## 0.32.32 展现形式优先配色试验版

- 新增“展现形式优先配色”：先确定媒介本体、材质、时代、环境与光线，再自然推导配色。
- 不预设固定色板，也不恢复暗色比例、亮度指标或上一轮配色强制纠偏。
- 限制不同媒介被统一套成同一种科技界面、终端界面、玻璃面板或低照度模板。
- 高级感继续由构图、材质、空间、光影、细节与色彩关系建立；配色仍服从展现形式。
- 0.32.31 的硬启动输出锁、0.32.30 交互急救、纯文字健康 UI 保护与 Visual Scenery 规则均保留。

## 0.32.31 兔子镜硬启动锁修复

- 修复 `hardStartup` 设置长期存在但未参与 Prompt 构建的问题。
- 在注入最前部加入短启动锁，要求先预留兔子镜输出长度；在末尾保留唯一完整输出契约。
- 若输出长度冲突，要求缩短正文或兔子镜内部文案，而不是省略整个兔子镜。
- 0.32.30 的 querySelector 后代状态急救、0.32.29 的健康 UI 保护及全部既有功能保持不变。

## 0.32.30 querySelector 后代状态急救

- 智能交互急救现在可从原始消息安全解析 `this.querySelector('固定选择器').style.xxx='固定值'`。
- 目标先在原始兔子镜内定位，再映射到已渲染节点，兼容宿主给 class 自动添加 `custom-` 前缀。
- 同一原始 onclick 已被精确恢复时，不再叠加通用“遮罩揭示”路线，避免双重监听和提前展开。
- 只读取固定选择器与固定样式赋值，不执行模型生成的 JavaScript；目标不能越出当前兔子镜。
- 保留 0.32.29 的健康 UI 保护、纯文字急救隔离、Visual Scenery 规则及全部既有急救。

## 0.32.29 纯文字急救健康 UI 保护

- 修复纯文字急救在没有 CSS ERROR 时，仅因检测到 `var(...)` 就预防性改写正常兔子镜的问题。
- CSS 变量定义在主容器 inline style、引用在局部 `<style>` 时，会从整条兔子镜收集变量后再展开，不再误替换为 `initial`。
- 无法可靠解析的变量保持原样，绝不再用 `initial` 覆盖背景、文字色与强调色。
- 保留 0.32.28 的输出契约、Visual Scenery、全部交互急救与其他功能；Prompt 未修改。

## 0.32.28 延迟相邻元素交互急救

- 修复 onclick 先改变自身、再在 setTimeout 内修改 `this.nextElementSibling.style...` 时，只建立“元素自变化”路线却未揭示相邻内容的问题。
- 安全解析相邻/前置/父级元素的最终样式赋值，不执行模型生成的 JavaScript，并将目标限制在当前兔子镜内部。
- 保留 0.32.27 的输出契约、Visual Scenery 规则、纯文字急救及全部既有交互急救。

## 0.32.27 首轮输出契约后置修复

- 修复 0.32.26 删除末尾保底后，强制生成兔子镜的要求只出现在注入开头、可能被后续长规则稀释的问题。
- 保持“只出现一处”的设计，将完整输出契约移动到注入末尾；每轮正文结束后必须继续生成兔子镜。
- 不恢复重复规则，不修改 Visual Scenery、扫描、纯文字/代码块/交互急救与其他功能。

## 0.32.26 规则精简与动态画面本体

- 压缩“复杂交互视觉核心”，保留媒介本体、空间层级、视觉锚点、明显交互、骨架避让与触屏兼容，不降低视觉硬指标。
- 删除重复且效果有限的“UI 自查短版”及其设置项；实际输出扫描、近期视觉避让和风险纠偏继续保留。
- 移除未实际控制 Prompt 的 `forceInteractiveMode` 旧设置；内部交互仍由常驻复杂视觉核心保证。
- Visual Scenery 改为“完整动态插画场景”：主视觉或环境层持续运动，禁止静态画加少量粒子冒充动画。
- Visual Scenery 的交互必须融入整幅画、局部景物或透明热区，不得另加独立按钮、面板和说明区。
- 删除重复动画短句、重复最终保底和 UI 自查；将每轮必出兔子镜的要求合并进唯一强制输出协议。
- 放宽 `inline style`：普通静态局部可内联，动画、响应式结构和状态联动可使用局部 `<style>`。
- 缩短内部 details 冷却文案；急救库和 0.32.25 纯文字职责隔离保持不变。

## 0.32.25 纯文字急救职责归并
- 只调整纯文字急救与代码块急救的职责，不改 Prompt、Visual Scenery、交互急救或其他规则。
- 删除代码块急救中遗留的 `CSS ERROR` 检测与“原文无变化也强制重绘”入口。
- `CSS ERROR` 检测、CSS 变量展开、纯文字强制重绘与重复重绘保护，现在全部只受“纯文字急救”开关控制。
- 保留共用底层：`<style>` 保护与 `<br>` 清理、HTML/CSS 紧凑化、`:root` 局部作用域、消息即时重绘。
- 因此关闭纯文字急救后，代码块急救不会再仅因画面出现 `CSS ERROR` 而暗中重绘；真正的代码围栏、`<pre><code>`、裸 HTML 仍由代码块急救处理。

## 0.32.24 纯文字急救
- 新增独立“纯文字急救”开关：兔子镜出现 `CSS ERROR` 或退化成纯文字时，自动从原始消息回读并重绘。
- 将宿主旧 CSS 解析器不支持的 `--自定义属性` / `var(...)` 展开为直接值，再删除变量声明；支持中文变量名、嵌套引用与 fallback。
- 对常见的根 class 漏写点号进行保守修复：仅当 `oldcity {}` 可明确对应 `.rabbit-scenery-oldcity` 时才改写。
- 可与代码块急救、智能交互急救串联，顺序固定为：纯文字 → 代码块 → 交互。
- 保留 0.32.23 全部急救库与 Visual Scenery 可见动画规则；Prompt 文件未修改。

## 0.32.23 Visual Scenery 可见动画兑现修复

- 修复“源码里有 `animation/@keyframes`，实际看起来仍像静态图”的伪兑现。
- Visual Scenery 现在要求打开后 1 秒内，至少一个主视觉层出现肉眼可见的位移、旋转、缩放、遮罩或光影变化。
- 仅有 3～5px 微尘、同色弱光、低对比小点或极慢小幅漂移，一律不再算动画。
- 要求关键帧保留元素原有 `rotate/scale`，或改由外层承载，避免 `transform` 覆盖使画面退化。
- 保留 0.32.22 的全部交互急救、CSS ERROR 重绘、`:root` 外壳恢复与取消暗色审查基线。

## 0.32.22 直接三元状态提前退出修复

- 修复安全状态程序在没有 `const x = this.checked ? ...` 变量声明时过早返回的问题。
- 现在可正确接管 `this.nextElementSibling.style.opacity = this.checked ? '1' : '0'` 及同类 `transform`、括号属性、文字赋值。
- 仍只解析当前兔子镜内的有限 DOM 亲属链与安全样式，不执行模型生成的 JavaScript。
- 保留 0.32.21 全部急救库与 0.32.20 Visual Scenery 持续动画硬约束；Prompt 不变。

## 0.32.21 相邻元素三元状态急救

- 新增对 `this.nextElementSibling.style.xxx = this.checked ? ... : ...` 的安全回读与重建。
- 同时支持有限的 `previousElementSibling`、`parentElement` / `parentNode` 链及点号、方括号样式写法。
- 不执行模型输出的 JavaScript，目标始终限制在当前兔子镜内部。
- 保留 0.32.20 Visual Scenery 持续动画硬约束及 0.32.19 全部急救库。

## 0.32.20 Visual Scenery 持续动画硬约束

- 修复 Visual Scenery 仍受“动态可选”规则影响、模型可能只输出静态画面的冲突。
- Visual Scenery 每轮必须包含至少一组自动运行、持续可见的 `CSS animation + @keyframes`，并使用 `infinite`；仅 transition、hover、checkbox 切换、SVG animate 或 JS 均不能代替。
- 保留 0.32.19 全部交互急救、0.32.17 外壳变量修复，以及 0.32.18 取消暗色审查的基线。
- 通过压缩原有 Visual Scenery 文案抵消新增约束，相关 Prompt 总字符数不增加。

## 0.32.19 `getElementById` 安全状态程序急救

- 修复 `onchange` 被宿主删除后，`const x = document.getElementById('...')` + `if(this.checked){...}else{...}` 无法恢复的问题。
- 仅在当前兔子镜内部解析固定 ID 与样式赋值，不执行模型 JavaScript，也不访问整页 DOM。
- 覆盖多目标同步变化：`transform`、`background`、`boxShadow`、`opacity` 等可随 checkbox/radio 切换并可逆恢复。
- 保留 0.32.18 的“取消暗色/配色审查”基线与 0.32.17+ 全部急救库；Prompt 文件未改。

## 0.32.18 取消暗色/配色审查，保留 0.32.17 急救库

- 取消读取上一轮实际颜色并注入“明度、综合色相、冷暖、饱和度必须变化”的配色节奏纠偏。
- 取消暗色主底、浅色主底与综合色族的自动历史判定，不再因为上一轮颜色强制本轮改用另一类底色。
- 删除“严禁单一套用黑底发光/紫蓝渐变”等暗色定向审查句；UI 自查与结构去模板化仍保留。
- 视觉扫描器不再采集或写入 paletteFingerprint，只保留结构骨架、媒介本体与交互风险检查。
- 完整保留 0.32.17 急救库：三类交互补全、可点击后置内容、CSS ERROR 即时重绘、`:root` 外壳变量恢复。
- 未增加 Prompt；相较原包反而减少配色纠偏注入。

## 0.32.17 `:root` 变量外壳恢复

- 修复完整 CSS 已恢复、但 `:root` 自定义变量在酒馆消息 CSS 隔离后失效，导致背景、卡带外壳、标签纸、进度条、文字配色与宽度消失的问题。
- 将消息内 `:root` 改写为当前兔子镜专属的稳定局部作用域，并同时标记 `<toto>` 与首个真实内容根；宿主移除未知 `<toto>` 时仍能继承变量。
- 每条作品使用独立作用域，不会让一条兔子镜的变量污染其他消息。
- 保留 0.32.16 的可点击后置内容急救与 0.32.15 的 CSS ERROR 即时重绘；`src/promptBuilder.js` 与全部 Prompt 数据未修改。

## 0.32.16 可点击后置隐藏内容急救

- 修复 `cursor:pointer` 的普通 `div/span` 后紧邻 `display:none` 正文时，因不是 `<button>`、也没有弹层关闭按钮而完全不命中急救的问题。
- 新增“可点击后置内容”独立路线：点击一次显示，第二次恢复；支持触屏点击与键盘 Enter/Space。
- 该路线只接管带明确“点击/确认/查看/封存”等提示的可点击容器，并排除绝对定位弹层和已有关闭按钮结构，避免与现有弹层路线重复。
- 一次性诊断新增“可点击后置内容”统计。
- 保留 0.32.15 的 CSS ERROR 即时重绘修复；`src/promptBuilder.js` 与全部 Prompt 数据未修改。

## 0.32.15 CSS ERROR 即时重绘急救

- 修复完整 `<style>` 首次渲染时被酒馆 Showdown 换行处理后出现 `CSS ERROR: ... missing '}'`，只剩纯文字的问题。
- 原始消息压缩完成后，调用酒馆原生 `updateMessageBlock` 立即重绘当前消息，不再依赖刷新页面或重新进入聊天。
- `<style>` 保护槽额外清除 `<br>` / `<br/>` / `<br />`，兼容不同换行写法。
- 保留已展开的 `<details>` 状态；不新增 Prompt，不改三类交互急救。

## 0.32.14 三类交互急救补全

- 新增“元素自变化”急救：宿主删除 `onclick` 后，仍可恢复 `this.innerHTML / innerText / textContent` 与 `this.style.*` 的安全固定值变化；普通交互保持可逆。
- 新增 checkbox/radio 的三元状态程序急救：支持 `this.checked ? A : B`、多层 `parentNode/parentElement + querySelector`、被净化器改成 `custom-` 前缀的 class，以及受限的 `style.cssText` 固定声明。
- 新增“可点击容器内部揭示”急救：同一容器内的点击提示与隐藏正文可独立展开、再次点击恢复；多个容器互不串联。
- 加强 label 点击状态确认，避免部分移动 WebView 在急救后又把 checkbox 切回原状态。
- 一次性诊断新增“元素自变化 / 安全状态程序 / 容器内揭示”路线统计。
- `src/promptBuilder.js`、主题库、展现形式库与全部 Prompt 数据未修改。

## 0.32.12 checkbox / radio → ID目标显隐急救

- 新增安全解析：`document.getElementById('id').style.xxx = this.checked ? 'A' : 'B'`。
- 动作绑定到 `input/change`，兼容隐藏 input 的 label 手动切换，不依赖原始 `click` 事件。
- 每个控件只操作当前兔子镜内对应 ID，多个控件与多个目标可独立开关。
- 交互完全可逆；不执行模型生成的 JavaScript。
- 一次性诊断新增“ID目标显隐”命中信息。
- Prompt 相关文件未修改。

## 0.32.11 可点击画面—相邻弹层可逆急救

- 新增“带 cursor:pointer 的画面 + 紧邻隐藏弹层 + 弹层内关闭按钮”结构识别。
- 点击画面打开弹层，点击关闭入口恢复，可反复打开/关闭。
- 不依赖已被宿主删除的 onclick；优先于普通 hover/pseudo 兜底。
- 一次性诊断新增“可点击画面弹层”命中项。
- Prompt 相关文件未修改。

## 0.32.10 按钮后置隐藏内容可逆急救

- 新增普通 `button` 后紧邻隐藏内容的渲染后急救。
- 点击一次显示，第二次点击精确恢复；不会照搬模型脚本把按钮永久隐藏。
- 此路线优先于触屏 hover 兜底，避免只改变按钮颜色却不显示内容。
- 一次性诊断新增“按钮后置内容”命中信息。
- Prompt 注入文件与 0.32.9 保持一致。

## 0.32.9 可逆交互＋Label 内弹层急救

- 新增统一可逆状态基线：第一次接管时保存原始样式与文字，取消交互时精确恢复。
- 修复宿主克隆或重建当前 DOM 后，监听器丢失、交互停留在打开状态的问题；新 DOM 会重新绑定，但不会把打开后的样式误记成初始状态。
- 同一个 checkbox/radio 只允许一条渲染后结构急救路线接管，避免多个兜底重复写样式。
- 新增 label 内“触发文字隔在中间＋隐藏绝对定位弹层”识别；显示时临时转为不被 overflow 裁切的流式内容，第二次点击恢复原弹层样式。
- label 后置结果层与相邻隐藏内容可点击结果区返回；遮罩揭示也支持 DOM 重建后继续开关。
- 一次性诊断继续保留，并显示当前结构型急救路线。
- Prompt、配色节奏、内部折叠冷却与 Visual Scenery 规则均未修改。

## 0.32.8 遮罩揭示＋后置结果层急救

- 新增无表单控件的“遮罩层＋隐藏正文层”点击/轻触揭示急救，覆盖被宿主删除 `onmouseover/onmouseout` 的结构。
- 新增 `label + checkbox/radio` 后方紧邻单块结果层的显示急救，并可增强同画布中的零尺寸圆形视觉主体。
- 设置页按钮强制横向排版，手机端不再逐字竖排。
- 一次性诊断继续保留，并新增上述两条急救路线的命中信息。
- Prompt 规则未修改。

# RabbitMirror 0.31.92

本版以 0.31.91 为底座，仅增强智能交互急救。

- 修复宿主在 DOM 渲染前删除 `onclick` 后，0.31.91 无法再读取伪交互程序的问题。
- 急救器会从当前聊天消息的原始 HTML 中回读安全可解析的 `document.getElementById(...)` 样式与文字赋值。
- 通过兔子镜标题与元素子路径映射回当前渲染节点，目标仍严格限制在当前兔子镜内部。
- 不执行原 JavaScript，不使用 `eval`；包含未知语句时仍然拒绝。
- Prompt、Visual Scenery、主题库、设置键、代码块急救均未修改。

详细变更见 `README-0.31.92.md`。

## 0.32.5 正式修复版
- 移除 0.32.4 的临时可视化诊断面板。
- 新增 label 内单块隐藏内容急救：排除隐藏 input，识别同一 label 中真正的隐藏正文。
- 勾选时同步修复 opacity、visibility、transform、display、height、max-height 与 overflow 裁切；取消后恢复原样。
- 兼容宿主删除 onchange 的情况，不执行模型生成的 JavaScript。
- Prompt 与 0.32.4 保持一致，不增加模型输入规则。

## 0.32.7 正式内置诊断版
- 保留 0.32.5 的 label 内隐藏内容急救。
- 新增默认关闭的“交互诊断面板（反馈用）”开关。
- 开启后在每个兔子镜底部显示版本、命中路线、事件、控件状态、隐藏候选、实际样式与裁切信息，并可一键复制。
- 关闭开关后移除诊断面板与诊断事件监听。
- 诊断只在浏览器本地运行，正文仅显示截断片段，不上传数据。
- Prompt、配色节奏、折叠冷却与 Visual Scenery 规则均未修改。


## 0.32.7 一次性轻量诊断
- 将常驻“交互诊断面板”改为“开始一次交互诊断”按钮。
- 只监听聊天区下一次被点击的兔子镜；捕获约 650ms 后自动停止全部诊断监听。
- 报告保留在当前兔子镜底部，可复制“诊断＋原始源码＋实际渲染代码”，也可关闭或重新诊断。
- 仅在点击复制时读取源码；超长 data URI / Base64 自动省略。
- 不修改 Prompt，新增诊断逻辑不增加模型输入 token。
