# 心迹回廊

## r62.0：阅读与生成修复（最新说明）

- 共同回忆按原图比例完整展示，说明放在图外；ADV 手机选择区不再大块吸顶，桌面和横屏可图文并排。只看 CG 隐藏说明和正文，返回阅读保留段落，不触发生成。
- 绘图支持最多两个不同目标同时提交；相同目标防重复，各自进度/取消。柏宝绘自身仍可能排队，实际并行取决于其设置和渠道；取消不代表上游立即停止计费。旧图成功保存前保留，逐张合并防覆盖。
- 日常一格实际绘图请求补充 Q 版比例和各格动作/镜头差异；允许一篇完整内容，不强制多篇或三个意象词。不会自动重绘已有图片，也不能保证模型每次完全遵从画风。
- 邮件不再显示阅读底部的解释，信纸有六种稳定配色；路线和私人终端不再夹入“旧版自由文字/证据未重新核验/角色日常演绎”。仅改展示，不改变原内容或内部来源属性。
- 终端内容按聊天、账本、笔记、联系人、音乐、相册等类型展示，不伪造音频播放或未生成照片。档案入口卡片同列宽；插件设置区不再放重复引导面板，从魔法棒进入。
- 前世今生/蝴蝶效应不再因两人关系旁白省略名字而误拦；明确的第三方恋爱仍不接受。蝴蝶不再按记忆条数凑节点，也不按固定汉字或代词次数验收。缺字段、损坏 JSON、连接或存储错误仍明确报告，成功内容保留。
- 恢复错误说明使用固定分类，不展示模型原文或密钥，也不再只显示 `[hidden]`。旧草稿只在身份、设置、原请求可精确核对并通过当前校验时复用；不匹配时停下说明，不自动重新收费。

本版承接下方 r61 功能；冲突以本节为准。构画接口适配的未完成状态不变。验证范围为本地代码、模拟宿主/提供方与浏览器视口；真实手机酒馆与实际模型/柏宝绘出图仍需使用端确认。

## r61.0：联合改版使用说明（由上方 r62 节补充）

- 入口：酒馆魔法棒 → 心迹回廊。首次显示独立设置首页；以后同聊天恢复模块、选中图片与阅读位置，顶部“首页”随时返回。切换聊天不串档；打开、恢复、切换房间不自动请求模型。
- 私人终端逐条接收：有记忆优先用，否则按人设/世界观写日常推演。成功项保留，缺项在终端“补齐”入口处理；截断、认证、保存错误仍保留恢复状态，不把推演回填正式聊天历史。
- 邮箱允许心情、日常来信、未来邀请；“明天一起看看去年我拍的照片”不再误判为共同往事。真实共同历史仍需来源。
- 暂停新增宠物及房间宠物必填；旧宠物不删除。房间物件、图标、编号、热点与列表同源。
- 记忆来源：最近 X 楼／指定范围／全部楼层；可选择隐藏普通对话，保留酒馆原楼号。外部摘要与自动激活世界书单独开关，选书无需先扫描插件。请求前显示实际聊天条数、字符量与额外上下文；字符量不是精确 token/价格。范围缩小不删除已归档内容；派生模块仍使用已保存档案。
- 独立手动 API 支持流式／非流式。流式截断保留可恢复正文；不会自动切模式再收费。主动取消/超时未交回的片段不保证可恢复。
- 每张图片一个“图片设置”，共同回忆为主按钮；日常一格按原比例完整显示，可打开原图，不重绘/不改变原画风。生图仅支持柏宝绘公开 API v1，旧渠道图片继续可读。
- 后日谈展示分段和结尾强调色，不改旧文字；主题显示名为初叶绿、海盐蓝、花漾粉、杏糖橙；萤火虫保留“个性话题”。
- 新增前世今生：依据档案意象创作独立虚构篇章，世界观适配引子／卷宗／缺页／今生回响／旁批／落款。无最低份数或字数，不强制答题；允许略过探索。真记忆与未来可能明确区分，不修改主聊天。新篇追加、旧篇保留。
- 备份失败显示固定分类与阶段代码，不输出私密正文；不自动删除/重建数据。黄色提示本身不能证明原档案丢失。
- **构画 ST-SevenDaysCal 适配未完成**：2026-09-13 核对官方 master 固定提交 f5be421421dcb4624b091d89782762d9c35a46cb（3.7.0）及另一落后分支，没有发现其对外公开的版本化只读记忆接口。它调用柏宝书不等于它自身提供同一接口；未访问私有存储、未改第三方插件。待作者提供实际接口提交或文档再接入。
- 更新：解压后把 tokimemo-main **里面的内容**覆盖现有扩展目录（manifest.json 同层），不要额外套一层，不要删除用户站点数据。刷新后从魔法棒进入。内部 heartbeatMemories/V3/gs1–4 标识保持兼容。

下方 r60 与更早章节是历史记录；如与本节入口、渠道、宠物和名称说明冲突，以本节为准。

## 历史 r60.0：创作补充词、画面提示词和继续生成

- 设置 → **创作补充词**：默认关闭，可填写最多 20,000 字符，点击保存后生效。只随缘侧自己的独立文本 API 请求发送，不注入主聊天、不发送给柏宝绘。它是创作偏好，不是安全等级，也不替代真实往事来源校验。
- 设置 → 标签过滤：支持 `版权水印` 等中文、英文和混合标签的输入、扫描、保存与过滤。扫描不会执行标签内容。
- CG 图片上的 `CG IMAGE` 遮挡已去除；相簿绘图操作移到图片下方。相簿、共同回忆、ADV、日常一格可打开“画面提示词”。“重新构思画面”只消耗文本请求，依据当前事件重新整理画面；确认后才绘图，新图成功保存前保留旧图。“重绘图片”沿用上次实际使用的提示词。
- 未检测到柏宝绘公开 API v1 时，会提示更新、启用并刷新，可重新检测；不猜测版本、不自动切换其他渠道或重复出图。
- 同一页面关闭/重开缘侧会尝试恢复同聊天的模块、选中条目和阅读位置；档案版本变化或原条目已删除时安全回到入口。刷新整个网页不保证恢复浏览位置。
- 截断后在档案入口显示“继续生成”；认证、限流等显示“重试未完成部分”，需要先解决相应配置/额度问题。通过原生产校验的分段保留，只补未完成分段，不拼接 JSON 尾巴、不降低历史来源校验。预览中的省略号仍是预览，不触发付费续写。
- 模块草稿按聊天、角色、档案版本和任务归属保存到现有本地备份；更换来源或创作规则不能悄悄重做成功项。请勿清理浏览器站点数据。
- 续写只适用于本版实际捕获并保留的草稿；升级前已丢失的截断正文无法凭空恢复。旧私人终端已有的有效分项草稿仍按原入口兼容。
- **首次建档/档案简介的整理草稿仅当前页面暂存**，关闭缘侧可保留，刷新网页会丢失未提交的整理进度。简介失败不撤销已保存的记忆，之后可只重试简介。

更新：将包内 `tokimemo-main` 的内容覆盖到原缘侧扩展目录（以含 `manifest.json` 的那层为准），不要额外嵌套一层文件夹，再刷新页面。旧正式档案、旧图与 V3 存储 key 不迁移、不自动重建。本次不包含“前世今生”模块，尚在方案阶段。

测试说明：本地断言与浏览器模拟不等于真实 TT/iPhone/模型/图像服务验收。任何“继续”都可能产生文本 API 费用，图片仅在明确确认绘图后请求。

## r59.0：柏宝绘公开接口适配

1. 单独安装并配置带公开 API v1 的柏宝绘（本次核对 test 分支 0.2.5）。
2. 覆盖更新缘侧后刷新页面，打开设置中的「CG 生图」，将「生图渠道」选为「柏宝绘 · 公开 API v1」。旧设置默认仍是 SillyTavern Image Generation。
3. 相簿 / ADV 点「绘制 CG」，或角色互动的日常一格点「绘制日常一格」，确认后才会消耗出图额度。可查看进度并取消；关闭弹窗可继续等待，页面退出后不保证继续。

仅使用作者公开的 STBaiBaiImage 接口，不复制、修改或打包柏宝绘，也不读取其密钥、私有配置或聊天 DOM。只发送当前条目的可见画面提示和固定的原角色图库分组名，不扫描聊天或自动读取角色库；没有额外的 LLM 转 tag 请求。角色外貌仍以该条目已保存的画面描述为准。

成功图片存入柏宝绘图库；缘侧保存本地图片路径，不把 base64 图片塞进聊天缓存。请勿在图库中删除仍被缘侧引用的图片。路径保存失败会明确提示并保留旧图，不自动再次出图；取消或超时后若柏宝绘尚未结束，会阻止重复请求。用户仍可手动切回原来的生图渠道。

本地 API 模拟、浏览器保存和隔离回归不能替代真实柏宝绘、TT / iPhone 或图像质量验收。接口依据：[作者公开协议](https://github.com/baibai-git/ST-BaiBai-Image/blob/bb223c52dbaea251fd3ab0135dddd4222c444127/PUBLIC_API.md)。

## r58.0：你的邮箱 · 生活终端 · 关系简介

- 档案室点“你的邮箱”即可开空箱，不发请求。“收取新信”按最新关系事件与本地日期去重；自动收信沿用设置中的逐模块楼层规则，默认关闭。
- 阶段来信覆盖初识、逐渐熟悉、确认关系，以及争执、冷战、和好、告别等实际剧情节点，不是固定好感度升级。每次收信最多一封最新节点来信和一封当日日常信；信件不替角色确认恋爱，也不兑现尚未发生的邀约。
- “收进路线明信片”零模型请求，保留收藏时的文字和 SVG 风景。可以已读、收藏与筛选；路线更新／删除不会改动已收信件。整份档案删除／明确完全重建仍会清除派生邮箱，请先备份。
- 私人终端“补齐缺项”保留成功条目，逐 App 保存。若另有成功续写草稿，会先提示从档案室继续草稿，不清除它。私人号码、真实过往消息和 User 已发台词仍不能凭空编造。
- 房间“更新人物外形”只重读外形证据，不重做正文、物件与宠物；未知外形显示光影外的存在提示。旧版本已经丢掉外形证据时需显式点一次此按钮。
- 档案简介改为约80～180字的双方态度／关系／分歧短简介。旧简介不自动覆盖；点“重写简介”才更新。
- 本地测试和浏览器模拟不等于真实模型、TT/iPhone 验收；尤其人物美观与文字气质仍需试用反馈。

## r57.0：日常演绎 · 终端续写 · 档案简介

以下 r57 及更早段落保留作版本历史；与本轮明确变更的规则冲突时，以顶部最新版本 / CURRENT 为准。

- 修复房间/路线/终端误把当下对白、未来邀请判成共同过去的问题；保留真实往事来源校验。
- 路线恢复原创对白与信件，不再只有统一枚举短句；旧终端成功项继续保留，只补未完成项。
- 封面改为能读懂双方关系与走向的文学简介，按内容自动选择九类文风。旧档案点“重写简介”即可，不用重建记忆。
- 本地回归不代表真实 TT / iPhone / 供应商输出已验收。请先备份现有插件与档案，在测试环境覆盖同一插件目录，刷新页面后试用。不要删除旧档案，不要重复安装成第二份。
- 阶段收信功能这轮没有实现。

## r56.0：房间 · 出行路线 · 私人终端生成规则回归

- 恢复 r45 的生成语义：角色自己的日常、当下对白、未来意向，以及依据角色卡/世界观作出的合理生活推演，不再因为缺少逐字 Mxxx/设定引文而被误判失败。
- 真正声称 `{{char}}` 与 `{{user}}` **过去已经共同发生**的内容仍保持严格证据边界：必须走 `basis=记忆` 并通过现有 Mxxx/anchor 校验；不会把未来愿望或普通“你/我们”字样当成历史。
- 私人终端的设定/推演条目保留模型生成的角色生活内容，不再用设定原文覆盖正文；聊天/联系人仍是证据型入口，不能凭空制造用户台词、号码或关系人。
- 出行路线在角色设定没有逐字地点时允许按人设合理推演，并明确保持为“推演”；关系层过滤不会因为只剩视觉动作而把整个地点误删。
- 本轮不改档案结构、V3 key、CAS、删除栅栏、连接、并发、超时、导航锁或其他模式。真实 iPhone / TT / 模型结果仍需真机确认。

## r55.0：封面判词 · 终端恢复 · 连接错误

- 档案封面先理解双方关系，再生成简短判词，不再铺陈剧情总结；旧梗概保留在默认折叠的“查看记忆梗概与索引”。旧档案点击“写下判词”即可，不需要删除或完全重建。
- 私人终端不再硬凑应用数或 8/12 句对话；只收录有据内容，少量双向原话也可保存。没有直接来源的槽位明确留空，不当作历史事实。旧有效记录不会被空结果覆盖。
- 草稿显示安全的失败原因和进度，重新打开后可继续未完成应用；已完成内容保留。全部来源不足不会卡在 N/N，来源改变也不会静默删掉已完成内容。
- 认证失败停止当前组合请求；限流只对失败段有界重试并遵守等待窗口；修正 HTTP 200 内嵌错误和混合错误措辞的分类。真实 Key/权限/余额问题仍需用户处理。
- 本地候选，未发布远端、未安装到用户 TT、未调用真实模型。见 RELEASE_VALIDATION-r55.md；真实文学效果、模型服务与手机宿主仍需实测。

## r54.0：TT 兼容与生成修复

- 关闭档案室直接转后台，保持本页开启时可切换聊天；不再依赖 TT 原生确认框。
- 设置文字与复选框不被宿主固定高度/装饰层裁切，保留原主题与纸张层次。
- 隐藏普通历史楼层不再误触发旧消息被修改的校验；每项自动更新仍按授权间隔运行，缺少任务锁会明确显示不可用。
- 房间不再错误要求动物画/职业里出现的宠物；出行路线按真实地点数量生成。目标档案明确选择的设定世界书也提供给这两项，不混入别的聊天。
- 保留旧档案、图片和缓存。原先已漏收历史的档案不做静默重建。实际 TT 手机和模型成功率仍待实测，详见 RELEASE_VALIDATION-r54.md。

## r53.0：配色层次 · 折叠设置 · 自适应蝴蝶效应

- 恢复黄色便签、蓝色备忘、粉色特别备注、紫色随笔与暖色纸页；页面、卡片和纸张有不同层次。四款 GS 灵感色板增强主色，保留正常字重、正文对比度和日夜模式。
- 设置中的独立 API、标签过滤、主题、自动更新、记忆来源默认收起，点击展开。折叠不清除正在填写的内容；档案入口和更新按钮仍可见。
- 初次蝴蝶效应按可引用的独立记忆条数生成：每 3 条安排一个普通分歧，上限 8 个，另有 MAIN 和 Ω，共 3～10 个节点。无可用锚点不发请求；已存节点不删减，仍可增量追加。
- 本地候选，不会自动发布仓库；真实 SillyTavern / TT / iPhone 与模型质量仍需验收。详见 RELEASE_VALIDATION-r53.md。

## r52.0 历史更新：生成修复 · 独立终端 · 可选自动更新

- User 的话单独显示气泡；动作与他人台词继续拆开，旧稿只修展示、不改原文。
- 房间逐字段、逐组补齐；空间和宠物同时缺失也能依次修复。增量只追加有据物件，保留旧房间。
- 蝴蝶初次生成改成十个节点逐个请求，失败只修当前节点，最后统一校验。不是删除校验，也不承诺所有服务商必定成功。
- 私人终端从房间移到独立档案入口；旧内容、未完成草稿继续使用。
- 设置 → 跟随当前聊天 · 自动更新：每项独立开关和 1–1000 楼间隔。默认全部关闭；可只开 ADV，档案不更新也能扩写。需要已有档案、页面运行及独立 API；不会自动生图。
- 内置“检查并更新插件”：强制检查当前项目仓库，Git 安装可拉取；ZIP 安装、权限或服务器 Git 故障仍需手动处理。此本地候选没有替你推送到远端。
- 新增初叶绿、海盐蓝、花漾粉、杏糖橙四套 GS1–4 灵感色板，与原四主题共八种选择。

使用与验证见 [r52 验证说明](RELEASE_VALIDATION-r52.md)。朋友建议只做 [功能取舍](FEATURE_DECISIONS-r52.md)；柏宝绘标签研究见 [接入说明](IMAGE_INTEROP-r52.md)，尚待仓库地址。无需删除或完全重建旧档案。

以下按版本保留历史说明；与当前行为冲突时，以本节及 CURRENT 的 r52 增补契约为准。

## r51.0：房间校验 · 对话归属 · 界面留白

- 修复房间把物件名称、描述、台词混在一起校验而误删正常物件的问题；生成与重新打开均适用。
- 蝴蝶效应三个生成入口共用完整要求，重试会收到安全的具体缺项提示，保留原有证据和关系校验。
- 动作与台词分开显示，明确的用户/NPC 不再使用 char 气泡；旧缓存未知归属显示旁白，不修改原文。
- 恢复浅色卡片层次、正常正文及留白，删除指定冗长说明；保留四种主题。
- 同时最多 10 个逻辑任务；模型请求仍最多 2 个，排队不会限制关闭插件窗口。

安装与验证边界见 [r51 验证说明](RELEASE_VALIDATION-r51.md)。无需删除旧档案或完全重建；覆盖更新后刷新酒馆，再单独重试失败项。真实服务商和 iPhone/TT 验收仍待用户测试。

## r50.0：同档案扩写 · 四种主题 · 共同陈列柜

- 没有新记忆也可主动扩写 ADV、相簿、平行分歧、角色互动、既有类型后日谈和出行文字。旧内容保留，不自动更新正式记忆或提升关系资格。
- 人际庭园单独读取本档案勾选的设定世界书；未登场 NPC 与已发生关系分层。历史摘要书仍只作为历史来源，不冒充人物设定；读取失败不覆盖旧庭园。
- 房间进入有界本地校验与重试，错误显示具体安全原因；蝴蝶效应只补未通过节点，继续保留世界差异和关系安全校验。
- 新增“两个人的陈列柜”：只收录能逐字核实的共同物件，点击查看原文和 Mxxx 来源，无证据就空着。
- 日间、夜间、跟随酒馆、自定义四种主题。跟随读取宿主标准计算背景与文字颜色，不复制第三方私有主题或任意 CSS；自定义提供预设起点、颜色选择与透明度预览。
- 设置新增“标签过滤”：扫描当前聊天标签、选择或输入排除标签、确认保存。默认排除 thinking、updatevariable、updatevarible；只过滤交给模型的副本，不改聊天正文或已存档案。
- 隐藏心跳使用四种不同本地交互结构，由角色与告白意象选择；模型只生成文字，不执行代码。分段并发上限调整为 2，仍受全局 provider 上限 2 约束。

安装与验收边界见 [r50 验证说明](RELEASE_VALIDATION-r50.md)。真实 iPhone/TT、用户服务商和真实大档案耗时仍须实机验收。

## r49.0：深度修复 · 跨聊天档案生成

- **已有档案可以在后台继续生成。** 从档案室明确选择 A 后会冻结 A 的角色、聊天、档案版本与允许上下文；切到 B 聊天不会取消 A，也不会让 A 读取或写入 B。A 被更新、重建、删除或启动同模式新任务时，旧结果自动失效。
- **TT 派生内容保全链重新收口。** 内存、压缩 metadata、本机备份和待提交记录继续按同一角色 / 聊天 / `archiveRevision`、生命周期与删除栅栏校验；ArchiveTarget 在每个异步边界沿用发起时的生命周期，扩展销毁后的迟到读取、请求和写入都会被拒绝。
- **Connection Profile 与手动 API 严格隔离。** “1.1.18 一键配置”和“手动配置”保持同级醒目；B 拉模型失败只保留 B 自己已存模型，手动线路也只使用手动配置。Heartbeat 可控错误统一脱敏。
- **第三方读取只走登记的公开接口。** r49 退役了直接读取 EverMind 私有 settings / metadata / API Key 的旧自动适配，也移除了按全局函数名、任意 `extensionPrompts` 或 `chatMetadata` 字段猜记忆来源的启发式 reader；已经确认进 Heartbeat 来源账本的资料仍可读，其他用户可继续用显式文件导入。柏宝书只走公开 API v1，矛盾的完整度/总数声明会降级为部分读取而不能替换旧完整基线。
- **一个受控 World Presentation 服务全部表现层。** 出行、房间、背影小人、宠物、私人终端和节日贺卡共享同一证据决策；角色卡 / 当前世界书优先，精确复核的 Character Profile 次之，再到两段不重叠正式档案共识，证据不足保持中性。电影、梦境、虚构和否定描述不能洗成世界事实。
- **日历与检索继续保持安静。** 日历只有角色写下的 memo、待办、备注、随笔和安全节日贺卡；标签支持多选、清除和状态保持。旧 Calendar、Phone、Travel 数据原样迁移或明确降级，不伪造缺失证据。
- **缓存里的表现 token 也要重新过白名单。** Travel 的 tone / mapTheme 与 Calendar 状态圆点在最终渲染前再验证；房间的当下生活与共同往事分开。“今天/现在”不会放行夹带的过去叙述，整段必须逐分句证明为当前态；共同过去只有 exact Mxxx 且可见文字包含真实锚点时才显示。
- **反查用户私人终端未硬做。** 当前证据结构无法稳定区分可展示的用户自述与不应推断的私人事实，r49 选择安全阻断。

## r48.0：两个人的日历 · 世界观节日贺卡

- **只在世界观中明确存在的节日出现。** 节日来自受控角色卡 / 世界书 / Persona 固定日期；没有可靠节日就不出卡，不为了功能凭空造节日。
- **不是固定贺卡模板。** 模型只给受限 art direction，本地 renderer 用安全 SVG primitive、HTML 与 CSS 重新构图；文字、图画、书写、图文、极简表达同级，正文和落款都可为空。
- **表现跟世界观走。** 贺卡可以使用受控的 card / paper / letter / scroll / folio / screen 载体；具体内容继续结合 {{char}} 人设、节日与当前 archive 关系状态，不建立“某性格=某形式”的固定映射。
- **不伪造历史。** 贺卡属于 Calendar Derived Content，不写回 Mxxx；世界观有某个节日不等于两个人过去一起过过该节日。
- **Calendar v6 安全迁移。** r47/v5 逐日内容继续保留；日期格只显示低干扰贺卡标记，选中日期后显示成品，不堆玩法注释。

## r47.0：TT 持久化增强 / 世界观表现 / Heartbeat 独立主题

- **派生内容优先完成持久写回。** 生成成功后会在显示完成前尝试把当前派生缓存压缩、写入同档案独立备份并提交 metadata；无法安全提交时继续使用已有待写回机制。完整 runtime 打开档案时，同一 `archiveRevision` 下如果本机备份比聊天缓存更新，会恢复较新的派生缓存；普通酒馆启动仍不会为此打开 IndexedDB。
- **错误正文不再由 Heartbeat 直接回显。** HTML/Cloudflare 等上游页面只显示状态与安全分类，API Key、Authorization、Prompt、聊天、档案和响应正文继续禁止进入 Heartbeat toast/日志。宿主 SillyTavern 自己产生的后端 toast 不在插件控制范围内。
- **出行、房间与私人终端更贴世界观。** 远方纪念载体新增书信、手札、卷轴、任务记录、数据记录等受控类型并兼容旧明信片；房间不再把帽子当默认人设推导；私人终端增加低技术与特殊世界观载体，并按 App 类型使用不同的本地信息结构。模型仍不能提供可执行 HTML/CSS/JS、任意 URL、class 或坐标。
- **档案标题更短。** 新生成标题要求 4～14 个汉字、像私人回忆册章节名；旧档案不会被自动改名。
- **新增 Heartbeat 独立主题。** 默认配色固定为珍珠白、雾蓝灰与低饱和粉紫/青绿；可选跟随宿主基础前景/背景或自定义调色盘。卡片透明度只改变卡片表面，不会连文字一起变淡；主文字和次文字会同时对不透明及合成后的卡片背景做 4.5:1 对比度保护。关键背景、文字、按钮、输入框和横排布局使用插件自身作用域，避免被普通宿主美化 CSS 覆盖到不可读。跟随模式只读取浏览器标准计算样式，不调用第三方美化插件私有设置或接口。
- **继续精简界面说明。** 功能页不堆玩法注释，只保留必要标题、状态、安全确认和短操作提示。
- **本版没有用删校验的方式实现跨聊天后台生成。** “聊 B、为已存档 A 生成”、反查终端和兔子镜标签过滤仍留给后续基于明确世界线/隐私边界的审查，不在 r47.0 中硬接。

## r46.0：通用记忆接入 / 来源存档 / 生成结果保全

> 历史说明：本节记录 r46 当时的行为；其中 EverMind 私有配置自动适配与任意全局 reader 已在 r49.0 退役。

- **常用来源自动读取。** 通过带版本和能力声明的只读适配器识别酒馆 `1_memory`、柏宝书公开 API v1 与 EverMind；来源会绑定当前角色和聊天，并显示完整、部分、截断或失败状态。实验性的任意全局 reader 仍默认关闭，不把“碰巧读到”宣传为完整适配。
- **未知插件可以显式导入。** 设置页提供醒目的“自动读取”和“导入记忆”，支持本地 JSON、JSONL、TXT 与 Markdown；导入前先显示角色/聊天归属、内容样例、条数与字符数，并要求明确勾选“这是已经发生的历史/摘要，不是角色设定”，确认后才绑定当前聊天。未知 JSON 的非元数据字符串叶也会带字段路径进入预览；API Key、密码、Token 和连接配置会被排除并标为“部分读取”，既不会藏在预览后面，也不会进入账本或模型。确认文件只导入该文件，不会顺带扫描其他插件。外部文字只作为资料解析，不执行其中的 HTML、脚本、宏或提示指令。
- **心跳回忆拥有独立来源账本。** 导入原文分片、稳定来源 ID、版本、覆盖信息与内容散列写入本机 IndexedDB；生成预算与完整来源存储分开。技术来源 ID 由完整 provider 身份生成，超长且前缀相同的文件名不会互相覆盖。对方插件卸载或停止注入后，已导入资料仍能从本机账本回读；用户可单独清除当前聊天来源，不影响酒馆聊天、第三方记忆或正式 Mxxx。
- **世界书用途不再混淆。** 默认仍把所选世界书当作设定解释；只有用户明确标记为“历史摘要来源”的条目才可作为摘要级历史证据，并保留世界书和条目锚点。历史来源按书独立保存；旧版合并来源会原子迁移，精确取消某个 UID 会立即生效，即使下一次读取暂时失败也不会把已取消条目重新送入生成。
- **修复记忆静默丢料。** 外部记忆真正获得预留空间；长摘要按片段保存，不再悄悄截成 6000 字；provider 原始记录 ID、revision 和 coverage 会被保留。没有正文但已有明确绑定历史来源时也可以建档。
- **生成切聊与刷新更安全。** 当前聊天离开确认与全网页未保全工作分开判定；CG、今日生活不再错误锁住别的聊天。原生确认框在移动 WebView 中不可用时会放行宿主切换/关闭，不再把窗口锁死。已经完成但尚未写回原聊天的结果会进入浏览器本地的有界待提交记录（最多 24 项、3.5 MB、7 天），回到原聊天后再按原角色/聊天/档案版本写回。备份或缓存等待结束后会再次核对角色；完全克隆、同名或同头像角色也按角色卡槽隔离，同毫秒产生的新结果不会被旧 ACK 误删。页面卸载会覆盖全部仍在飞行或未保全的工作。
- **远方明信片按地点绘景。** 每个地点使用白名单风景类型；旧数据会从地点名称、区域和简介安全推断。海港与雪山不再因为共用地图主题而画成同一种景色，桌面和移动端构图也不再裁掉关键海面或地面。

## r45.0：独立 API 双配置 / 连接稳定性修复

- **两个入口同级醒目。** 设置页直接显示“1.1.18 一键配置”和“手动配置”两张心跳回忆粉蓝卡片，只保留短状态、必要字段和模型按钮，不照搬兔子镜 UI。
- **一键配置先验能力。** 配置写入、模型刷新和正式生成前都会检查 Connection Profile 的 Secret 转发与请求级模型覆盖能力；能力不完整时在付费请求前停止。入口与错误提示统一标注 `1.1.18`。
- **手动 OpenAI 兼容通道。** 可填写 Base URL、API Key 与模型 ID，也可显式拉取模型；浏览器始终只请求 SillyTavern 固定同源 `/status` / `/generate`，不会直接跨域访问第三方 URL。带 Key 的远端地址要求 HTTPS，本机 loopback 开发地址仍可使用 HTTP。
- **修复主副连接串线。** Profile B 拉模型不再借用主聊天 Profile A 的 custom headers；同一个 Profile 重复一键配置保留单独选定的模型，换 Profile 才清除旧模型覆盖。
- **受控 JSON 更稳定。** Profile 请求改用结构化 messages，并关闭主聊天 preset / instruct 注入；手动响应支持常见可见正文形态、字节上限和安全错误分类。默认等待提高到 10 分钟，绝对上限 20 分钟。
- **配置竞态关闭。** 一键配置、模型列表和生成结果都绑定配置 epoch / 去敏指纹；等待时切换来源、修改模型或停用 runtime，迟到结果不会覆盖新设置或写入缓存。

## r44.0：感情扫描共同回忆 / 独立出行地图 / 背影房间与告白彩蛋

- **共同回忆先扫描双方感情基础。** 相簿严格按“CG 索引 → 完整档案关系扫描 → 当下共同回忆对白”生成，分别判断 `charState`、`userState` 与当前关系阶段；单方心意不会被误写成双方已确认。单张重新生成也执行同样扫描，并把安全快照随条目保存。
- **日历成为角色自己的只读手账。** 页面只展示 {{char}} 的备忘、待办、特别备注与页角随笔，不再提供访客输入框、添加按钮或手动勾选入口；旧缓存里的草稿和手动待办保留在数据层以避免升级丢失，但不再显示。
- **他的房间只画背影，并真正区分生活空间。** CSS 人物不再包含眼睛、嘴或正脸几何；空间会按职业、兴趣、时代与物件母题显示不同陈设。受控角色卡/世界书明确有某类宠物时，候选漏掉对应宠物会被拒绝并修复重试；档案宠物仍需 exact Mxxx。没有依据时不会凭空添加，也不会把别人的宠物归给当前角色。
- **人际庭园增加 NPC 视角。** 点击非用户 NPC 头像后会显示该 NPC 对 {{char}} 的视角；动态关系优先于固有设定，旧缓存缺少视角时明确提示刷新，不会冒充秘密或新增事实。
- **成就显示解锁条件。** 已解锁成就同时展示“如何解锁”和解锁时间；旧缓存安全回退到原证据锚点或说明。
- **新增档案室独立“他的出行路线”。** 它不是手机 App：近地图地点点击后播放 {{char}} 的逐句对白，远地图地点打开完全由本地 CSS 与文字构成的明信片。私人终端会过滤地图、导航、路线、出行与交通类 App。
- **告白回看加入本地心跳彩蛋。** 弹窗提供触碰心跳、悬停核心、解锁短句、暂停/恢复日志、稳定信号等交互，并实时更新视觉、日志与诗句。模型只返回经清洗的结构化文字，不能提供或执行 JavaScript、HTML、CSS、URL 或事件处理器。
- **旧档案保留，必要时显式补全。** 旧房间会显示“补全宠物与视觉设定”，即使没有新 Mxxx 也可扫描人设/世界书，不删除旧物件或深层内容。如需用新差异校验完整重做旧房间，使用顶部管理器的“重新生成整个分类”，并注意确认框中的依赖内容重建提示。旧 CG 台词可用单张重生成升级感情扫描。
- **历史身份与异步导航保护。** 相簿、人际庭园和明信片不会借用另一聊天的角色/用户；手动分类即时刷新身份缓存。地图标记不重叠，切换页面/聊天时相簿延迟和 Ending 彩蛋会安全收束。

## r43.0：人设化房间与终端 / Calendar v5 逐日手账 / 蝴蝶内容强化

- **两个人的日历升级为 Calendar v5 逐日页。** 每个具体日期、无年份纪念日和待定约定都有独立页面；草稿、便签、特别备注、页角随笔与手动 To-Do 只属于当前页，不再由所有日期共用。没有剧情事项的空白日期也可以点开并使用自己的页面；长期积累超过 480 个页面也不会被迁移逻辑静默截断。
- **旧日历迁移不扩散内容。** v4 根级便签/随笔只在日期、日历项或可靠档案证据能确定归属时移入对应页；无法确定的内容完整保留在“旧版未归档”页，绝不会复制到每一天。迁移和逐日编辑都只作用于派生 Calendar session，不改写正式 Mxxx 档案。
- **房间和房中人物按人设变化。** 世界书/角色设定中有明确线索时优先采用；缺少明确描述时再根据人物时代、身份、职业、生活方式与气质选择代码内置的安全房型、配色、陈设及 CSS 人物轮廓和脸型比例。明确设定字段与人设推导字段分开，模型照抄合法示例也不会让全员变成同一套造型。房中人物不套用档案头像，也不接受模型提供的 CSS、HTML、URL 或坐标。
- **私人终端恢复完整设备层级。** 先看到有状态栏、壁纸和 App 图标的设备主页，点 App 进入独立列表，再点条目进入详情；设备类型、主题配色、图标风格、App 组合和排序会随人物与世界观变化。手机、腕式设备、终端和传讯器使用各自形态，不复制真实商业产品界面。
- **蝴蝶效应只强化内容，不重做 UI。** 原有分歧树、配色、观测页、SIGNAL 动画与观测点 Ω 保持原状；生成会读取角色卡、User Persona 与相关世界书，至少给出 8 个实质不同的平行人生。本地在保存前检查第一人称长独白、现世回应、冷静的 SYSTEM NOTE、Ω 终局以及第三方恋爱禁令，不合格结果不会覆盖旧内容。

## r42.7：ADV 第一人称心情补完 / 界面注释精简

- 移除萤火虫栖息地标题下及旧光点内的静态玩法说明；标题、光点数量、分页、五色图例与会话 / 旧心声内容继续保留。
- 移除 Character Profile 生成卡与已生成资料卡下的静态说明；角色头像、姓名、固定资料、生成 / 重读按钮及模型生成的人物简介继续保留。
- 单篇与批量 ADV 统一为结构化 `narrator + sections` 输出，正文必须以角色第一人称为主，重点补完角色的性格、动机与情绪。
- 每篇 ADV 必须从过去、日常、共同经历时的当时心情、后日谈四类中实质覆盖至少两类；每类必须有足够正文，不能只声明标签。
- 本地在保存前同时检查类别、段数、总字数与第一人称密度；不合格的批量项进入原有失败补全流程，不会覆盖已经保存的合格内容。旧 ADV 缓存仍按原有 `paragraphs` 结构直接阅读。
- r42.6 档案室索引恢复、r42.5 独立备份 / CAS / 12 MB 保护和 r42.0 lazy bootstrap 全部保留。

## r42.6：明确重新建档后恢复档案室索引

- 修复删除过角色档案后，用户明确重新建档虽提示成功，却仍被旧角色墓碑隐藏，导致档案室显示为空的回归。
- 明确的首次新建档在正式档案与独立备份都成功提交后，只撤销当前角色的删除墓碑，并立即重建档案室索引。
- 对已经落入该状态的 r42.5 档案，打开档案室时会用 `createdAt > deletedAt` 做单向安全迁移：只恢复删除之后真正新建的档案，删除前的旧 metadata 仍会被墓碑拦截。
- r42.5 的独立备份、12 MB 保护、CAS、删除栅栏和 r42.0 lazy bootstrap 全部保留。

## r42.5：独立档案备份 / 超大缓存保护 / 版本 CAS

- 正式 Mxxx 档案与兼容的派生缓存会在用户显式加载完整 Heartbeat runtime、建档/更新或打开历史档案时，写入浏览器本机 IndexedDB 独立备份；不再只依赖源聊天 `chatMetadata`。
- 源聊天文件删除、丢失或无法读取时，档案室会尝试从独立备份恢复，并以“永久只读”方式显示。备份不会自动绑定到其他聊天；清除站点数据、换浏览器或换设备不会带走这份本机备份。
- 所有正式档案提交统一执行 `archiveRevision` compare-and-set。生成期间版本变化时，迟到的前台或后台结果会被丢弃，不会覆盖较新档案。
- 12 MB 上限统一按 UTF-8 字节执行到压缩、无压缩、延迟写回与销毁路径。候选缓存过大时保留上一份有效压缩/原始缓存，不再用不可读的超大 raw cache 覆盖它。
- 明确“双确认删除当前档案/角色档案”会一并删除对应 Heartbeat 本机备份内容，并留下不含档案内容的极小删除栅栏，阻止迟到的 seed/缓存任务把备份复活；“仅从档案室移除索引”仍不删除备份或源聊天数据。
- r42.0 轻量 bootstrap 不变：普通启动和零解压性能诊断都不会导入完整 runtime，也不会打开 IndexedDB。

## r42.4：性能诊断可收起

- 轻量 bootstrap 与加载完整 runtime 后的设置面板都支持再次点击诊断按钮收起结果，并提供明确的“关闭诊断”按钮。
- 展开状态通过 `aria-expanded` 同步，按钮会显示“关闭性能诊断”，不再出现打开后没有关闭路径的情况。
- 关闭只改变当前设置页 DOM 显示；不会解压缓存、不会请求 API，也不会让轻量 bootstrap 提前加载完整 runtime。

## r42.3：安全边界与长期状态收口

- EverMind 仅允许 HTTPS；HTTP 只对 URL 解析后确认的 loopback（`localhost`、`127.0.0.0/8`、`::1`）开放，不再把 Bearer API Key 发往远程明文地址。
- 剧场缓存写入和读取统一使用 12 MB UTF-8 字节上限；新 manifest 同时记录 `sourceChars` 与 `sourceBytes`。旧 manifest 继续兼容，零解压诊断只做 O(1) 标量读取和保守估算。
- disable / clean 会清空 preflight、历史 snapshot、模型列表和档案一览等瞬时缓存，并用 runtime lifecycle epoch 阻止旧异步结果、正在 gzip 落盘或 gunzip hydrate 的 Promise 在销毁后回填。r46 起，已经完成且等待回到原聊天写回的 durable deferred commit 不再随 runtime 销毁清空。
- 打开单篇历史档案改为只请求目标聊天，不再用 `metadata:true` 拉取同角色全部聊天 metadata。SillyTavern 当前接口仍会返回该目标聊天全文，但不会再按同角色档案数量放大。
- r42.2 移动端 bootstrap 排版与 r42.0 按需加载边界保持不变：启动和“性能诊断”都不会加载完整 runtime。

## r42.2：移动端轻量 bootstrap 排版修复

- iPhone / SillyTavern 窄屏设置页中的“加载完整设置”和“性能诊断（不解压缓存）”改为上下排列、接近容器全宽，中文保持水平且不逐字换行。
- 按钮触摸高度至少 46px；到 768px 以上才切换为双列，并对宿主 `.menu_button` 的收缩宽度样式做局部覆盖。
- 普通启动仍只执行轻量 `index.js`；性能诊断仍然不解压缓存、不加载完整 runtime，完整 Heartbeat CSS 也不会提前注入。

## r42.1：萤火虫改为 GS4 式追加约会会话

- 重新按 GS4「ホタルの住処」实际表现修正。现在一颗光不是一整段 {{char}} 内心独白，而是一段两个人当场展开的追加约会会话：{{char}} 会因为“能听见心声”的特殊气氛不小心把本音说出口，{{user}} 只有极短、非正史的中性即时回应，最后可出现一条“刚才是不是他的心声”的即时想法。
- 原四色恢复为：💗恋爱 / 💙恋爱的烦恼 / 💛朋友 / 🤍お楽しみ・角色个性话题；♥️ 仍保留，但明确属于本插件扩展的“直白渴望”。
- 首次和后续仍每批只生成 **5～6 颗**，单页最多 **6 颗**，永久库机制不变。旧版一句话或 2～4 段独白光点会继续显示，并可原地批量升级成会话，不要求删库重来。
- yellow/white 不再被误写成“关于他自己 / 脆弱与秘密”两类固定独白；朋友、工作学习、兴趣、梦想、食物、宠物、价值观等会真正承担角色自己的生活与人际内容，减少所有光点都在说 user 的问题。
- r42.0 的轻量 bootstrap 与零解压性能诊断全部保留。

## r42.0：酒馆零 runtime 启动 / 零解压缓存诊断

- 酒馆普通启动只执行轻量 `index.js` bootstrap，不再立即下载/解析约 1.17 MB 的 Heartbeat runtime bundle。第一次点“心跳回忆 · 档案室”或“加载完整设置”时才加载完整功能。
- 启动时仍会挂一个很小的档案室菜单和设置占位，因此插件入口不会消失；完整 runtime 加载后由正式菜单/设置接管。
- 轻量设置里可直接点 **性能诊断（不解压缓存）**。它只读取已经由 SillyTavern 放入内存的 metadata manifest：Mxxx 数量、派生 mode、`sourceChars`、可用时的 `sourceBytes`、压缩 Base64 长度和估算 gzip 大小。
- 诊断不 `atob`、不解压、不序列化整个 theater cache，也不遍历聊天正文；点诊断不会触发完整 runtime。
- r41.5～r41.9 的普通聊天性能收口、设置/API 懒填充、Drama/萤火虫、Character Profile 与单一人际庭园全部继续保留。


## r41.9：角色档案漏读修复 / 折叠 Profile / 一张人际庭园

- Character Profile 不再只依赖模型主动填写 `facts[]`。角色卡里明确写出的生日、年龄、身高、血型、职业等会由插件本地做保守字面抽取兜底；例如简介里已经出现“30岁、网文作家、影视编剧、前历史系讲师”，对应格不会再无故显示 `？？？`。
- `年龄` / `职业` / `学校` 等常见 AI label 会统一映射到 GS 资料格；User Persona 不再作为 {{char}} 客观事实来源，避免把 user 自己的年龄/职业串进角色档案。
- 已生成的旧 Profile 打开时会在安全匹配角色卡后自动补读明确的角色卡事实，不需要为了这种漏格重新请求 API。
- Character Profile 改成默认折叠：角色页首屏只占一条“头像 + 姓名 + 已读取 N/9 项”，点开才显示大照片、简介与全部资料。
- 角色总页不再重复显示一张“固有设定人际庭园”。每个聊天世界线只保留 **一张人际庭园**，在同一图中合并角色固定关系与该聊天后来发生的关系变化；持久化证据边界仍保持分离，避免跨窗口串关系。
- r41.5～r41.8 的启动/聊天性能收口、Drama 翻页、完整萤火虫心声与 5～6 颗小批次全部保留。

## r41.8：萤火虫小批次点亮

- 完整继承 r41.5 性能收口、r41.6 真机故障修复/启动减负与 r41.7 Drama 翻页 + 完整心声结构。
- 萤火虫首次点亮改为 **5～6 颗**；后续有新 Mxxx 时每批也只追加 **5～6 颗**，更像剧情推进后逐渐亮起少量新心声。
- 单页同时显示/发光最多 **6 颗**；永久解锁库上限保持不变，旧光仍可分页回看。
- 旧版一句话萤火虫升级也每批最多处理 **6 颗**，避免一次升级太多。

## r41.7：萤火虫完整心声 + 四季 Drama 可翻页

- 萤火虫每颗光点改为一个完整心声主题，包含 2～4 段自然递进的内心，而不是一句短句；旧光继续兼容。
- 首次点亮 18～22 颗，后续有新 Mxxx 时每批追加 8～12 颗；单页仍只同时发光 18 颗。
- 修复春夏秋冬 / 后日谈 `1 / N` 左右箭头无法真正切换 Voice / Scenario 的状态优先级错误。

## r41.6：首次 HEART 修复 + 启动再减负

- 修复 iOS Safari / WebView 首次生成 HEART 时的 `Can't find variable: data`。
- Connection Manager 与模型下拉不再在酒馆启动时同步填充，只在打开“心跳回忆”设置时读取。
- 聊天切换时档案按钮状态使用轻量判断，不扫描整段聊天。

在 r41.4 统一角色主档案基础上，仅做性能收口，不改变功能层级：

- 删除墓碑过滤改为一次 `Set` 索引，避免档案室在大量历史删除记录下出现 O(档案×墓碑) 放大。
- 萤火虫永久库仍保留，但单页同时发光由 30 降为 **18**。
- 普通消息事件更新设置按钮时走轻量状态，不再为每条消息遍历完整聊天。
- 启动仅注入设置面板小型 CSS；完整 Heartbeat UI CSS 第一次打开档案室时才注入。
- 仍然是一个 runtime bundle，不恢复多文件网络启动。

## r41.4：统一角色主档案 / Character Profile / 人际关系证据分层（r41.9 起 UI 合并为一张图）

- 同一个 SillyTavern char 的不同聊天窗口共用一个 **Character Profile**：大头像、人物简介，以及生日 / 年龄或年级 / 身高 / 血型 / 职业学校 / 兴趣喜好等 GS 风格资料。没有明确设定的客观值保持未知，模型猜测会被本地证据校验丢弃。
- 角色固定关系只读取角色卡、当前 User Persona 与受控世界书 dry-run。若 {{user}} 在故事开始前就被明确设定为青梅竹马、未婚约、主从、同事、亲属式身份、宿敌等，会作为 **全窗口共用的固有关系数据**；Persona 只写普通个人资料时不会自动制造特殊关系。
- 当前聊天世界线的恋爱、暧昧、同居、争执、和解以及后来认识的人必须引用当前档案真实 Mxxx + anchor，不能继承到同一 char 的其它窗口。r41.9 起角色总页不再单独画固定关系图；进入具体聊天时只显示一张合并图，但固定关系与世界线变化在存储/证据上仍严格分开。
- 人际庭园不使用虚假的 0～100 好感数值；显示关系状态、char 单向印象与证据锚点。布局坐标、样式和头像来源由插件本地控制，模型不能提供 CSS、URL 或坐标。
- 普通角色卡文字修改导致 fingerprint 变化时，只要姓名 + avatar 仍能唯一确认是同一个人，就继续复用原角色组与 Character Profile，不会平白拆出第二份角色档案。
- “删除角色档案”会同步移除这份全窗口共用 Profile / 固有人际数据，并继续保留双重确认与“不删除 SillyTavern 正文聊天”的安全边界。

- **逐渐解锁的人物资料**也纳入同一结构：公共 Profile 仍只接受角色卡 / Persona / 世界书明确事实；某个聊天窗口后来得知的生日、喜好、习惯、害怕的东西等只保存在该窗口的「这个世界线了解到的他」，必须回指真实 Mxxx，绝不自动提升为全窗口事实。

## r41.2：角色档案安全删除

- 档案室最外层角色卡可直接删除整个角色档案。
- 删除经过两次确认，只移除心跳回忆档案室中的角色卡、头像入口与该角色的聊天档案索引；SillyTavern 正文聊天窗口完全保留。
- 删除状态会阻止旧档案扫描或当前窗口 metadata 把角色自动重新加入档案室。

## r41.1：萤火虫永久解锁库 / 分页点亮

- 萤火虫从“重新生成整片”改为**永久累积**：旧光点不会被后续生成覆盖；只有用户显式删除、重建整个 HEART 派生内容或达到上限时才会变化。
- 首次仍点亮 24～30 颗；以后只有当前窗口档案新增了关系进展时，才调用模型追加 6～10 颗新心声。没有新 Mxxx 时不会白白调用 API。
- 新光点会记录本轮档案消费范围与批次，生成端同时读取最近旧心声做去重；本地还会拒绝完全重复的文字。旧 r41 已生成萤火虫但没有增量游标时，首次点击只建立一次保守基线，不覆盖旧光点。
- 为避免长期积累后在 iPhone / WebView 一次运行上百个发光动画，缓存最多保留 240 颗，但 UI **每页只渲染 30 颗**，可翻看较早/更新的光。达到 240 后停止自动追加，不会淘汰旧光点。
- 五色语义不变：💗 / 💙 / 💛 / 🤍 / ♥️；♥️ 继续表示对 {{user}} 的直白渴望。

## r40.2：日历变成“私人手账页”

- 月历仍然负责圈出真正重要的日期，但进入日历后，下面会直接看到 **便签夹 + To-Do List + 特别备注 + 页角随笔**，而不是必须先点某一天才看到内容。
- **To-Do List** 由剧情里仍未兑现的明确约定自动形成；正式档案后来记录兑现后，刷新日历才会进入已完成状态。
- **便签** 可以是基于真实档案的小提醒，也可以是来自角色卡 / 世界书的稳定设定备注；两者会明确区分来源。
- **页角随笔** 最多 3 条，只有存在真实档案锚点时才生成；它只是角色视角的短手账文字，不写回 Mxxx，也不会每个事件都配一条。
- 新增便签 / 随笔同样可以在“管理”中单独删除或重新生成，并继续要求连续二次确认。

## r40：真正的“两个人的日历”

- 日历不再把所有带日期的剧情记忆做成事件大纲。只有“角色真的会拿笔圈起来”的共同事项才会进入日历，例如接人、约会、一起出行、生日/纪念日、明确安排或约定兑现。
- “发烧、吃醋、剪衣领、争吵、临时插曲”等剧情经过，即使发生在有日期的记忆里，也不会因为有日期就自动成为日历事项。
- UI 改为真正的月历网格。被记录的日期会被圈出；r40.2 起，月历下方进一步改成便签、To-Do、特别备注与少量页角随笔的完整手账页。
- 已经明确约好的未来事项可以提前进入日历；具体日期必须能从真实档案证据中核对，否则只能保持“待定”，模型不能猜日期。
- 世界书/角色设定中的固定日期继续作为辅助“设定日”存在，但不会被当成已经约定或已经发生。
- r39.1 及更早的旧 Calendar 派生缓存会自动失效一次，避免旧版“剧情大纲式日历”继续显示；正式档案 `MEMORY_KEY` 不变。


## r39.1：日历直接显示在档案室首页

- 修正日历入口层级：打开“心跳回忆 · 档案室”后，首页直接显示“两个人的日历”横条；不再要求先点“打开当前窗口档案”。
- 当前聊天已有正式档案时，可直接生成 / 打开 / 刷新日历；尚未建档时也会显示日历入口，并明确提示先建立当前窗口档案。
- 日历数据模型、证据规则、Phone 增量、单文件运行时和既有缓存格式均不变。


## r39：云酒馆启动优化 / 单文件运行时 / Phone 增量显式按钮

- 模块化源码继续保留在 `src/`，但 SillyTavern 实际启动改为只加载 `dist/heartbeatMemories.bundle.js` 一个版本化运行文件，不再在云酒馆启动时请求 42 个 ES module。
- 移除 r38 为刷新子模块而加入的“每个版本首次强制整页 reload”；单文件 bundle 自带版本 query，因此不会再出现 manifest 已更新但 Calendar / Phone 子模块仍是旧缓存的混版。
- “两个人的日历”继续固定显示在当前档案第一屏，不再作为普通分类卡隐藏在下方。
- 私人终端内部增加明确写着“增量追加终端”的按钮；手机上不再只依赖顶栏一个无文字的 `↻` 图标。只读历史档案会显示“只读 · 无法增量”。
- 启动日志会记录当前 bundle 的加载耗时，便于云酒馆继续做性能排查。

## 0.8.14：日历首屏可见

“两个人的日历”现在是档案首屏独立入口，不再需要在普通分类卡里向下寻找。已有日历可直接打开，未生成时可直接生成；历史只读档案也会明确显示是否已经有日历。


当前版本：**0.8.58（r62.0）**


## r38：日历显示修复 / 终端双向对话 / 终端增量

- **修复更新后日历入口偶尔消失**：r35 模块化后子 ES modules 使用固定 URL，SillyTavern 原地更新时浏览器可能继续复用旧模块图。r38 的入口在检测到新 build 时只执行一次页面刷新，再动态载入本版本运行时，确保 `constants / snapshots / calendarView` 等全部来自同一发布版本。
- **私人终端聊天改成真正双向**：chat 条目新增 `contactName` 和 `speakerRole=owner|contact`。生成/重新生成时必须同时出现设备主人与聊天对象；缺角色或整段只有“对方”的结果会校验失败并重试。UI 以左右两侧消息卡显示两方，并用实际名字而不是“对方 / 我”占位。
- **旧聊天缓存不乱猜**：旧版已经保存且缺少可靠 speakerRole 的对话不会被本地静默改写；详情页会提示可通过“管理 → 重新生成这一条”修复。
- **私人终端恢复增量追加**：Phone 仍是 Room deep mode，但 r38 单独允许它显示顶栏“增量追加”；点击后复用已有 `generatePhoneIncrementalWithRepair()`，只消费尚未用于终端的新 Mxxx，旧 App / 条目原样保留。
- **日历、内容管理与固定名称继续保留**：两个人的日历仍位于 ENDING 与角色互动之间；r37 的二次确认删除/重新生成管理不回退；公开扩展名仍固定为“心跳回忆”。


## r37：统一删除 / 重新生成管理

- 每个已生成分类的顶部新增 **管理** 入口。可对整个分类执行“重新生成整个分类 / 删除整个分类”，也可向下管理可独立生成的子项。
- 单项覆盖：相簿卡与单张 CG、ADV EVENT 事件卡 / ADV 正文 / 单张 CG、Voice Drama、Scenario Drama、日常一格及其图片、私人终端 App / 单条内容、ENDING 路线 / 告白回看、成就、日历项、蝴蝶效应分歧；房间的“今日生活”也可单独重新生成 / 删除。Room / Items 结构本身保留分类级管理，避免把结构依赖拆成不一致的半个房间。
- **所有删除与真正的重新生成均连续确认两次。** 第一次说明影响范围，第二次是最终确认；任意一次取消都不会修改缓存。首次绘制新 CG 仍只做普通调用确认，但已有 CG 的“重绘”属于重新生成，必须二次确认。
- 单项重新生成采用“新结果先生成并通过校验 → 再原子替换旧项”。模型失败、聊天切换或档案 revision 变化时旧内容保持不变。
- 删除 / 替换只处理 `heartbeatMemoriesTheaterV3` 派生缓存；这里不会删除正式 `MEMORY_KEY` / Mxxx，不修改聊天正文或世界书。
- 整个“他的房间”被删除或重新生成时，会清除依赖旧房间结构的“他的物品”和“私人终端”派生缓存，避免旧深层数据挂在新房间上。
- 历史档案仍遵守只读门禁：只有当前真实聊天进入可写状态后才允许管理。


## r36：两个人的日历

档案室新增独立的 **两个人的日历**，入口位于 **ENDING → 日历 → 角色互动**。日历先负责整理时间状态，不自动生成节日剧情。

| 日历状态 | 含义 | 可信来源 |
|---|---|---|
| **已经度过** | 剧情里确实发生过、且档案记有明确日期的日子 | 本地直接读取正式 Mxxx；模型不能创建 past |
| **已约定 · 未发生** | 剧情中明确约好、但后续档案尚未记录兑现或取消的安排 | 必须回指真实 memory ID + anchor，通过 evidence 校验 |
| **未来 · 世界设定** | 世界观本身存在的生日、节日、纪念日等固定日期 | 角色卡 / Persona / 世界书；仅为设定，不代表两个人已约定或已发生 |

- 可按 **状态** 与 **月份** 筛选。没有明确日期的约定可以显示「待定」。
- 世界设定只有出现明确 `MM/DD` 或 `YYYY/MM/DD` 才进入未来日历；「冬季祭典」但没有日期时不会猜。
- r36 **不提供“点未来节日自动生成特别篇”**。这是刻意保留的边界：先区分事实、约定和设定，后续再决定节日特别篇的玩法。
- Calendar 是独立 mode，不塞进 HEART；它保存于派生 cache，不改正式档案 schema。


## r35.1：启动热修

- 修复 r35 模块化后入口只导出 `init()`、但 SillyTavern 并不会自动调用该导出，导致扩展文件已经启用却没有真正挂载菜单/设置的问题。
- 恢复 DOM ready 自动初始化；r35 的模块结构、缓存/档案 schema、ADV EVENT 兼容和所有功能行为不变。


## r35：模块化重构 / ADV EVENT

- 运行时从单一约 79 万字符的 `src/heartbeatMemories.js` 拆成 `core / archive / generation / modes / ui` 五层；入口文件只保留 init / destroy。
- 9 个业务 mode 之间禁止直接 import。共享的档案证据、revision fence、请求并发、缓存、文本净化与 Connection Manager 请求继续只有一套权威实现。
- 原 **CG/ADV** 产品名称统一改为 **ADV EVENT**；历史缓存仍使用 `MODE.ADV = "adv"`，无需迁移，旧事件、CG 图片与 ADV 全文继续可读。
- r35 是结构重构版本：除 ADV EVENT 展示名称外，不改变 r34 的 Prompt、生成数量、档案 schema、缓存 schema、TT/全屏行为、60k 最大输出、未来四季 Drama 或只读档案规则。

## r34：四季未来日常 / 时期对话头像专属

- 春、夏、秋、冬和未来/后日谈不再从档案新增 Mxxx 里挑关键词写故事。当前档案只负责确定关系阶段；四季正文是新的未来日常模拟，可随时继续追加。
- 剧情方向会在二人约会、居家日常、购物跑腿、散步出行、工作学习后的碰面，以及角色卡/世界书明确存在的朋友、家人、同事互动之间轮换。没有明确设定时不会凭空创造固定家人或重要 NPC。
- 角色互动页面只保留 **春夏秋冬 / Drama** 与 **日常一格**。早中晚、周末、生日、节日和久未访问等“各种时期的对话”继续存在，但只在档案室点击角色头像时按状态出现。
- 节假日特别篇暂未加入 r34；现有头像节日台词保持原样，后续可单独设计为世界观日历驱动的节日番外。

## r33：干净构建 / TT 显示切换 / 生成上限修复 / 角色互动独立入口

- CG 实图仅使用 SillyTavern Image Generation 的 `/imagine`、`/sd`、`/img` 公开路径和手动 `/sd` 兜底。
- 设置新增 **TT 显示模式**：勾选时使用 r32 的移动端顶部安全区；不勾选时恢复长期使用的全屏 edge-to-edge UI。默认不勾选。
- 修复 r32 生成回归：副 API 的“最大输出”重新作为 Connection Manager 的真实 `maxTokens`；各功能遗留的 3k/3.8k 等分段提示不再暗中压低用户设置。
- 档案室把 **角色互动** 作为独立入口，顺序放在 **成就库** 前面。


## r32：一键安装酒馆 / iPhone 全屏关闭修复

- 全屏档案室会避开 iOS 刘海、灵动岛和状态栏安全区；即使某些一键安装 WebView 错误地把 `env(safe-area-inset-top)` 报成 0，也会启用 iOS 专用顶部兜底。
- 手机顶栏返回、主页、追加与关闭按钮的触摸目标由 34×34 增至 44×44。关闭按钮增加仅限自身的 `pointerdown/touchstart` 兜底，不再只等待可能被宿主吞掉的 `click`。
- 兜底不会拦截整个页面或酒馆侧滑手势；关闭只隐藏心跳回忆界面，正在生成的任务仍继续绑定原角色、原聊天与原档案版本。

## r30：所有内容都在原有基础上增量增加

先像以前一样点击“增量更新当前窗口档案”，让正式档案追加新的 Mxxx；然后在想扩充的内容入口点击“增量追加”。每一项都有自己的本地消费游标，只读取尚未用于该项的新 Mxxx。没有新记忆时会直接提示，不会调用模型，也不会把旧内容再讲一次。

| 内容 | r30 的追加行为 |
|---|---|
| 回忆相簿 | 追加新 CG 节点和共同回忆；旧描述、评论与已绘制图片保持不变 |
| ADV EVENT | 追加新事件索引；旧 CG 图片和已完成 ADV 全文保持不变 |
| 他的房间 / 物品 | 向既有空间、容器追加新证据物件，必要时增加新空间/容器；旧文字不改 |
| 私人终端 | 在既有 App 后追加新条目；旧聊天、照片文字档案、联系人等不重做 |
| 蝴蝶效应 | 追加新平行分歧；上一轮 Ω 保存为历史观测，再生成新的 Ω |
| ENDING / 告白 | 追加新路线或新解锁阶段、终章、后日谈与已发生告白；旧路线逐条保留，关系旧状态进入历史 |
| 成就库 | 追加新里程碑；旧的未解锁目标可在新证据下升级为解锁 |
| HEART 对话 / 一格 | 分别追加新时期对话、特别日和日常一格 |
| 未来 / 春 / 夏 / 秋 / 冬 | 每一季有独立游标；同一季可以保存多篇 Voice / Scenario，补春篇不会消耗夏秋冬的新档案 |

四季第一次各只有一篇完全没问题。以后每次档案增加后，再点对应季节，就会在该季后面增加下一篇。若 Voice 已成功而 Scenario 失败，成功的半篇会先保存；再次点击只补同一批的缺失半篇，不会重做旧篇。

r28/r29 的旧缓存可直接升级。第一次档案增量时，插件先把更新前所有 Mxxx 记成旧内容基线，再让新增 Mxxx 保持待处理；若无法还原旧快照，则保守地把当前档案视为旧基线，避免误复述，下一次档案增量即可正常继续追加。

派生集合采用旧项优先的 240 项安全上限；满额后不会为了腾位置删除旧内容。“今日生活”、设备四时段状态等本来就代表当前状态的字段仍原位更新，它们不是篇目收藏。完全重建正式档案会重编号证据，因此仍会清空不再兼容的派生缓存；普通增量更新不会。

## r29：最大输出 60,000 真正生效

- 设置页“最大输出”现在允许到 **60,000 tokens**，保存/读取不会再被旧 30,000 常量压回。
- JSON 响应的本地解析容量同步提升到 600,000 字符，避免 60k token 长输出先被插件自己截断。
- 报错会同时显示当前插件设置和该功能本段实际请求上限。比如全局设 60,000，但春季 Voice 本段代码只申请 3,000 时，提示会明确显示本段仍是 3,000。
- 60,000 是插件上限，不代表所有模型或代理都接受 60,000；Provider 拒绝时仍按原错误分类处理。

## r28：生成时降低网络占用

- 模型请求进行中不再反复把整个剧场缓存写回 chat metadata；手机 App、Drama Voice/Scenario、ADV 等局部成功会先保存在同一聊天的 runtime cache，等 provider 请求队列空闲后再合并 gzip 并持久化一次。
- 支持 `CompressionStream` 的现代浏览器不再执行“先保存未压缩大缓存 → 再保存压缩缓存”的双写；只有不支持本地 gzip 的旧浏览器才保留未压缩 fallback。
- provider 并发上限仍为 2，r27 的未来/春/夏/秋/冬独立排队、Voice/Scenario 分段与局部成功保存逻辑不变。
- 这项优化只改变派生缓存的持久化时机，不改变正式档案、证据锚点、跨聊天/revision fence；如果页面在生成尚未结束时被强制关闭，尚未完成 idle flush 的最新派生片段可能只存在于本次运行内存中。
- “未来 / 后日谈”是单段 Voice，不再错误显示 `1/2`。

## r27：春夏秋冬可同时排队 / 单季 Voice 与 Scenario 再拆分

- 未来、春、夏、秋、冬不再共享一个生成锁，可以连续点击多个季节。插件最多允许 5 个逻辑任务排队，但 Connection Manager 仍最多同时发送 2 个请求。
- 春夏秋冬每一季拆成两个独立小段：Voice Drama 与 Scenario Drama。成功一段就立刻保存；若只成功一段，页面显示 `1/2`，再次点击只补缺失的一段。
- 四季 Voice / Scenario 和未来 Voice 的最低长度适当降低，单请求最大输出也同步缩小，优先保证稳定生成而不是为了硬凑长篇导致整段失败。
- 并发完成时使用 HEART patch 合并，不会出现“春先成功，夏后保存却把春覆盖”的问题。

## r26：设置说明清空 / 终端续写 / Drama 逐季生成

- **设置页继续瘦身**：保留连接、模型、刷新、最大输出、温度、禁用词与两个勾选控件；删除勾选项下方从“专用连接：…”到“模型刷新只调用…”的整块灰色说明文字。
- **角色 / user 标签改真实姓名**：日常一格以及 Drama 剧本行不再显示“角色”“剧本中的你”，改用当前 char 名称和当前 user 名称；只读历史档案优先使用该档案保存的角色 / user 名称。
- **私人终端支持失败续写**：目录生成后，每完成一个 App 就保存精简、已校验的续写草稿。中途失败时房间按钮显示“继续生成… · 已完成/总数”，续写跳过已经完成的 App，不再从头请求。草稿只绑定同一 chatId + archiveRevision；档案更新或终端完整保存后自动清除。
- **春夏秋冬 Drama 改为逐季按钮**：未来/后日谈、春、夏、秋、冬各自单独生成并立即保存。某一季失败不会再让前面已经成功的季节全部作废。
- **r24 请求协调器与 r25 证据边界不变**：Provider 限流、超时、重试白名单、原子写入、memory ID/anchor 校验与只读保护继续保留。

## r25：重要节点增量收藏 / 三分角色互动 / 成就库


- **回忆相簿与 ADV EVENT 不再凑固定数量**：每次只抓当前档案里约 3～6 个最重要、最适合做 CG 的节点，与旧收藏增量合并。共同回忆每张 4～6 段，界面不再显示“现在的他 → 现在的你”。
- **ADV 一批最多 6 篇**：收藏可以一直累积，但每次批量正文只生成最多 6 篇，避免以后节点越来越多又把一个请求撑爆。
- **告白回看才使用头像逐句对话**：只有已经发生、能通过档案证据校验的告白进入播放器；ENDING 未来路线不再放头像告白。
- **角色互动分三块单独生成**：各种时期的对话 / 春夏秋冬·Drama / 日常一格。首次只做时期对话；四季再拆成后日谈与春夏秋冬小请求；日常一格独立生成。
- **私人终端更轻**：手机保留 9 类 App、总条目约 29 条，深聊为 2 个 × 12 条左右；不再生成手机内置日历，双方日期/约定统一交给独立「两个人的日历」。其它设备进一步按能力压缩。
- **档案室新增成就库**：已解锁 / 未解锁分区。已解锁必须有真实 memory ID + anchor；未解锁只显示条件和提示。
- **界面说明大幅精简**：保留必要状态、错误、只读和确认，其余重复说明删除。r24 的请求协调器、超时、限流和原子提交继续保留。

## r24：生成协调器修复 / 头像逐句告白

- **不会再被自己的分段挤满任务位**：一个相簿、私人终端、角色互动或 ENDING 只算一个逻辑任务；内部小请求由统一队列控制，同时最多 2 个真正发往模型。
- **不会无限转圈**：单次模型请求默认最多等待 5 分钟，超时后释放任务位并保留旧内容。认证、429、上下文超限、配置错误和上游故障会显示不同提示。
- **分段改为稳定顺序**：同一模式内部不再三路同时轰 API；结构目录使用较低温度，正文继续使用用户设置的创作温度。内容数量和本地校验门槛没有降低。
- **ENDING 是角色亲口告白**：可用路线用角色卡本地头像、姓名框和单句对话框播放 6～10 句第一人称告白，并提供上一句、下一句、重播。旧 ENDING 缓存也会本地拆句显示。

## r23：大生成项稳定化 / ENDING 并行提速

- **私人终端**：先生成设备/App/条目目录，再按 App 并行补详情；不再一次要求 65+ 条目和全部深聊。phone 保留完整内容量，手表/传讯器不再被错误要求和手机一样的 3 个 24 消息深聊。
- **回忆相簿**：15+ 张 CG 的目录/证据先生成；共同回忆按每 4 张一组并行生成，仍然是“现在的角色陪现在的用户看过去 CG”。
- **角色互动 / Voice Drama**：基础问候包、后日谈 Voice、四季 Voice、四季 Scenario 分段；后 3 段并行生成并分别校验。
- **ENDING**：目录与告白回看同时跑；已解锁路线正文最多 3 路并行。原来的证据锚点、失败关闭、旧缓存保护不变。
- 这些分段单次请求最高只需约 5.5k～12k 输出，默认 16,384 tokens 不再需要承担“一个 JSON 塞完整个模式”的压力；如果用户主动设置得更低，仍尊重用户设置。

## r22：ENDING 分段 / 共同回忆当下陪看 / 生图手动兜底

- **ENDING 不再一次生成全部**：先生成关系与路线目录，再单独扫描已发生告白，最后对已解锁路线逐条生成长篇终章与后日谈。这样单次请求更短；某条路线短请求失败会只重试该条，仍失败则不会覆盖旧 ENDING。
- **共同回忆不是 ADV**：点击相簿里的“共同回忆”后，定位是“现在的他和现在的你一起看过去的 CG”。新生成的相簿评论会直接对现在的你说话、评价当时与现在的变化，不再写成过去时长篇内心回放。
- **生图检测更宽 + 可手动确认**：除了 `/imagine` 也检查 `/sd` / `/img`。如果你的 Image Generation 明明已开启但自动检测仍失败，可在心跳回忆设置中勾选“手动确认 Image Generation 已启用”；绘制时会用受控 `/sd quiet=true` 兜底。

## r21：角色版本分类 / 禁用词 / 房间差异化

- **char / char 2.0 不再因为共用头像自动挤进同一组**：新写入或手动重扫的档案索引会保存角色卡内容指纹并优先按指纹分类；没有指纹的旧索引仍可在“管理角色分类”里手动拆分。
- **管理角色分类**：支持“一键自动分类未锁定档案”、把某个聊天档案移动到现有角色组，以及从 SillyTavern 当前角色列表里手动选择一个 char 新建角色组。这里只改心跳回忆的展示索引，不移动/重命名/删除酒馆聊天文件。
- **旧版本歧义不乱猜**：旧档案若没有角色卡指纹，而 SillyTavern 里又存在多个同头像/同名候选，会显示成独立的“待手动分类”。你可以逐条移动到正确组；手动归类以后自动分类不会再覆盖。
- **删除档案**：当前真实聊天提供“删除当前档案”，必须连续确认两次，只删除 Heartbeat 的 `MEMORY_KEY` 与相簿/ADV EVENT/房间/ENDING/HEART 等派生缓存，聊天正文不动。其它历史档案提供“从档案室移除”，只删轻量索引，不会偷偷切聊天或删除酒馆聊天文件。
- **生成禁用词**：默认禁用 `老子`。它只约束相簿 / ADV / 房间 / ENDING / HEART 等新生成派生文本；命中后本次结果失败关闭，不静默替换、不自动重试，历史聊天、正式档案和证据 anchor 原文都不改。
- **房间终于不是一个模子**：卧室、客厅、厨房、书房、音乐/录音工作室、实验室、浴室、餐厅、阳台/庭院、工作间、办公室、营帐/船舱等都有不同固定场景骨架，同类房间再有 3 种稳定布局变体。旧房间缓存若已有明确类型/名称，也会直接使用新布局，无需重生成。

## r18：r17 审计收尾与第三方记忆 reader 显式授权

- **五任务计数补齐**：ADV 批量/逐项恢复、房间“今日生活”和 CG/日常一格绘图都进入统一逻辑并发计数；批量入口在构建 Prompt 前先检查容量，真正发送请求时再做第二次硬检查。
- **第三方记忆 reader 默认关闭**：普通 SillyTavern Memory 注入摘要和当前聊天 metadata 摘要仍可直接作为补充来源；只有你主动勾选“允许调用第三方记忆插件公开 current-chat 读取函数”后，心跳回忆才会执行其它扩展公开的 current-chat reader。
- **Slash Command 兼容收口**：CG/日常一格生图和读取当前连接设置都走统一的公开 `NamedArgumentsCapture` 调用方式，不依赖或伪造 SillyTavern parser 私有参数。
- **只读命名/残留修复**：档案一览函数明确重命名为 snapshot-only 语义，关闭只读后的 UI 刷新不再传入不存在的参数；仍然不会自动切角色/聊天。
- **升级保底缓存不裁剪**：扩展关闭/重载时继续完整保留 runtime theater cache；大缓存只告警，不为了省空间丢相簿/ADV/房间/ENDING/HEART 等已生成数据。


## r17：档案状态修复、告白单独刷新与五入口并行

- **告白回看单独更新**：在 ENDING → 告白回看中点击“只重新读取告白”，只扫描后来新增/更新到档案里的真实告白节点。原有结局路线、逆转告白和后日谈不变。
- **五个主入口最多并行 5 项**：回忆相簿、ADV EVENT、房间、ENDING、蝴蝶效应可以同时各跑一个主任务；仍禁止同一个入口重复启动。
- **只读开关不再切聊天**：从全角色档案馆打开旧档案仍默认只读。关闭只读只让编辑按钮出现，不会自动切角色、聊天或刷新酒馆。真正写入时必须是用户已经手动打开的同角色/同 chatId live 聊天，否则只提示、不写入。
- **CG 生图动态重检**：相簿和 ADV EVENT 的状态条提供“重新检测”。先开档案、后启动 Image Generation 也可以立刻重新检测，不需要重开档案。
- **快照不能覆盖 live 缓存**：即使关闭只读，历史 snapshot 也只用于浏览；写入前必须重新取得 live session，缺失时 fail closed。

### r16：角色互动、Voice Drama、四季小剧场与日常一格

- 档案室的角色头像现在是互动入口。已有 HEART 缓存时，点击头像会依据本地现实时间、周末、角色生日/用户生日（仅设定明确时）/特别日以及“距离上次点头像多久”弹出符合人设的台词；3 天左右偏担心、7 天以上可闹别扭、14 天以上在关系合适时可轻度吃醋。点击头像本身不会发模型请求。
- 新增 `HEART / 角色互动与 Voice Drama`：包含后日谈长篇生活剧场、春夏秋冬 Voice Drama、春夏秋冬 Scenario Drama 和“日常一格”。这些内容是明确标注的附加演出/未来模拟，不写入正式 MEMORY_KEY 档案。
- “日常一格”先保存 1/2/4 格的文字分镜和台词；可选使用 SillyTavern 已配置的 Image Generation 绘制 Q 版漫画。图片 prompt 明确要求无文字/无对白框/无水印，真正台词由界面单独显示。
- ENDING 新增 `逆转告白` 路线。只有当前档案已有可验证的强烈依恋与“差点错过/竞争/吃醋/关系位置流失”压力时才会解锁；普通暧昧不会硬开。

## 0.8.10 r14：CG 生图入口可见性

### r15：只读保护开关与告白回看

- 从全角色档案馆打开的历史档案默认只读。关闭“只读查看”只显示编辑按钮，绝不会自动切角色、聊天或刷新宿主界面；真正写入前必须由用户自己在 SillyTavern 打开对应聊天，并再次通过角色 + chatId + 档案身份校验。每次增量追加仍有独立确认。
- ENDING 增加“告白回看”页，只从手动档案中已经发生、能通过 memory ID + anchor 验证的告白/关系确认生成回看。没有证据就不生成。
- “告白回看”是过去事件的档案式重构；ENDING 路线中的 confession 仍是未来终章推演，两者不会混淆。


- 回忆相簿中每张已解锁 CG 的缩略图右下角直接提供 **“🎨 绘制 / ↻ 重绘”**，不必先进入详情。
- ADV EVENT 详情增加 **“CG 实图 · Image Generation 已连接 / 未检测到”** 状态条，绘制按钮固定放在操作区最前。
- 如果从全角色档案馆点开的条目就是 SillyTavern 当前正在打开的同一角色、同一聊天档案，会自动回到可写的 live 档案；真正的其他历史档案仍只读，且不会为了绘图自动切聊天。

## 基本说明

当前构建发布通道：正式仓库 `https://github.com/Zaiyebuzuoyouqingdetiangou/tokimemo`。

> **Source Available — Not Open Source**
> 仅授权个人、非商业安装和使用未经修改的版本。禁止二改、提取代码/Prompt/UI/实现逻辑、制作衍生版本、转载、重新打包、镜像分发、整合到其他项目、商业使用，以及将仓库链接或受保护项目材料提交给生成式 AI / 代码生成工具 / AI Agent 用于仿制、替代、衍生开发或继续开发。详见 [LICENSE](./LICENSE)。




## 0.8.10 r12：CG 实图绘制

- 相簿已解锁 CG 和 ADV EVENT 事件都新增 **“🎨 绘制CG”**。如果 SillyTavern 已启用并配置官方 Image Generation 扩展，点击后会使用其 `imagine` 命令生成图片，并覆盖原来的抽象 CG；没有插件、生成失败或图片失效时继续显示抽象 CG。
- 绘制永远是手动动作，并会先确认可能消耗本地算力、额度或付费点数。当前版本不自动批量生图。
- 新生成的 CG 数据会额外带一个纯视觉 `imagePrompt`；旧缓存无需重做，会从原 `desc/cgDesc + visualSeed` 生成 fallback。心跳回忆不会把完整聊天、档案、世界书或私人终端内容交给生图扩展。
- 图片由 SillyTavern Image Generation 自己保存；心跳回忆只保存同源本地图片路径，不保存 base64，也不直接读取或管理任何生图服务 API Key。

## 0.8.9.1 独立入口并行生成

- 移除“生成整套档案室内容”一键基础包入口。回忆相簿、ADV EVENT 事件索引、他的房间、蝴蝶效应、ENDING / 后日谈都采用**各自首次生成 / 增量追加**，每一项只承担自己的 Prompt、游标与 JSON 校验，避免整包过长导致一次失败全部失败。
- 五个主入口共用最多 **5 项并行生成** 的安全上限。每个任务使用独立 AbortController、原 chatId、characterKey 与 archiveRevision；同一聊天切走后继续后台运行，完成后只写回原窗口。
- 同一入口禁止重复启动；不同入口可以同时请求。ADV EVENT 的具体长篇正文仍按单个事件生成，同一个聊天同一时刻只允许一篇具体 ADV，避免并发覆盖事件缓存。
- “他的物品 / 私人终端”继续没有档案室独立入口。进入“他的房间”后，如果对应深层内容尚未生成，按钮会变成“生成并翻找 / 生成并查看”，可与其他主入口同时后台生成。
- 并行任务完成时采用按 mode 合并的延迟写回，避免用户切到别的聊天后两个不同入口先后完成、后一个结果把前一个 deferred session 覆盖掉。
- 创建/更新聊天档案、重新读取记忆插件仍属于档案级操作：有内容生成任务时会暂时禁止，以防 archiveRevision 在并行任务中途变化。

## 0.8.10 r9：ENDING 与纯文字相册

- 私人终端 Gallery 不再读取/请求世界书里的外部图片地址，只保留照片分类、标题、预览、正文和 `imageCaption` 的文字照片档案。
- 档案室新增独立 `ENDING / 后日谈`。当前关系状态和每条路线都有真实档案锚点；结局场景与后日谈明确属于未来推演，不写回正式档案。
- 至少包含当前路线、恋爱、羁绊、开放四类结局。恋爱关系证据不足时显示未解锁条件，不会硬把暧昧写成已经交往。已解锁路线包含 3+ 段后日谈生活切片。

## 0.8.8 档案保留与聊天进入性能修复

- **插件版本升级不再等同于档案 schema 升级。** 0.8.8 继续使用稳定的 `heartbeatMemoriesArchiveV3` / schema v3；0.8.x 正常升级不会删除、重建或判废旧档案。只有用户主动“更新聊天档案”时，才会产生新的 archive revision，并按证据一致性失效旧的派生剧场内容。
- **已生成剧场内容继续保留。** 相簿、ADV、房间、物品、私人终端、蝴蝶效应仍跟随对应聊天保存；版本更新、初始化、切换聊天都不会主动清空。
- 修复电脑端进入长聊天时 CPU 突升的主要放大器：0.8.7 以前把完整剧场缓存作为巨大嵌套对象直接放进 `chat_metadata`。ADV 越多，聊天文件第一行越大；SillyTavern 打开聊天时必须整文件读取并 JSON 解析这些内容。
- 0.8.8 将**剧场缓存**改为 `gzip + Base64` 压缩后仍保存在同一个聊天 metadata 中，因此不会变成本机专属缓存，也不会因为升级插件而丢失；普通聊天打开时只需要解析压缩字符串，真正打开“心跳回忆”档案室时才解压。
- 旧版未压缩缓存采用**懒迁移**：第一次进入旧聊天仍能读取旧数据，浏览器空闲时会把它压缩后原位保存。第一次迁移可能仍有一次短暂 CPU 占用，之后同一聊天的进入成本会明显下降。
- 解压后的运行时剧场缓存只保留最近 3 个聊天窗口，避免长时间切换多个角色后把全部 ADV / 房间内容常驻内存。
- `CHAT_CHANGED / CHAT_LOADED` 在档案室关闭时不再做档案 UI 维护；长聊天的“可用消息数”也按 chat scope 缓存，仅消息变化时失效。
- 压缩读取增加体积上限，避免恶意或损坏的聊天 metadata 造成异常解压内存占用。

## 0.8.6 重点

- 当前聊天档案改为**全窗口覆盖优先**：长聊天不再只截取最近 36 万字符；超出硬上限时按整个窗口均匀取样，并保留原消息编号。
- 档案整理真正后台化：聊天扫描分批让出主线程，档案分块不再重复执行高成本 token 计数，进度只更新轻量状态文字。
- “共同回忆”由 3 句浅评论升级为 **6～8 段角色回想**。
- “他的物品 / 他的手机”不再占用档案室独立入口：两者都收回 **“他的房间”内部**，从私人空间里继续翻找收纳或查看符合时代设定的私人通讯终端。
- 档案室新增 **“档案室一览”**：当前角色的每个聊天窗口各自对应一份独立心跳回忆档案，可查看有无建档、档案名、记忆数量与更新时间，并安全切换到对应聊天。
- 当前窗口记忆桥接支持通用公开接口适配，但 r18 起**默认不执行第三方 reader**。用户显式开启后，可识别 `getInjectedHistory()` / `getCurrentChatMemories()` / `getCurrentChatMemory()` / `getCurrentChatSummary()` / `getCurrentSummary()` 与可选 `getSnapshot()`；调用前后都校验仍处于发起时的聊天窗口。
- 蝴蝶效应恢复原始“时间分歧树”设计：主时间线由真实档案锚定；8+ 平行外延节点改由角色卡 + User Persona + 相关世界书推演，最后附 TRUE ENDING 彩蛋；平行世界明确是模拟，不会写回档案。
- 0.8.9.1 起取消整套基础包请求：相簿、ADV EVENT、房间、蝴蝶效应分别生成，可最多 5 项并行；长篇 ADV 正文继续按具体事件点开后单独生成。

独立 SillyTavern 第三方扩展。与兔子镜无依赖、无共享运行时。

## 这版怎么生成

心跳回忆支持两种互斥的独立 API 来源，均不跟随主聊天连接自动切换：

- **1.1.18 一键配置**：引用 SillyTavern Connection Manager Profile，只保存 Profile ID 与可选模型覆盖，不复制 API Key；运行时按实际 Profile Secret / override 能力校验。
- **手动配置**：保存用户填写的 OpenAI 兼容 Base URL、API Key 与模型 ID。Key 使用 password 输入且不回填界面，只经 SillyTavern 固定同源 custom backend 转发。
- 两种来源不会在失败时互相回退，避免把请求静默发到另一个账号或模型。

- **连接配置**：一键模式决定 Provider / Server URL / Secret ID；手动模式使用显式 Base URL / Key。
- **模型**：两个来源分别保存模型选择，不修改主聊天模型。
- **刷新 / 拉取模型**：只在用户点击时调用 SillyTavern 同源状态接口；一键模式只传 Profile B 的 Secret ID，绝不借用主聊天 Profile A 的 custom headers。

如果 API 不支持列模型，或者服务端状态接口没有返回模型，插件仍会显示该 Connection Profile 本身以及同连接其他已保存 Profile 中出现过的模型作为备用。

## 设置页与档案室

0.8.6 把两种界面彻底分开：

- **插件设置页只负责 API**：1.1.18 一键配置、手动配置、模型刷新/选择、最大输出、温度与每日生活自动请求开关。
- **档案室是独立入口**：可从设置页的“打开档案室”按钮，或 SillyTavern 扩展菜单里的“心跳回忆 · 档案室”进入。
- 档案室负责创建/更新当前聊天档案、查看档案摘要，并提供 **档案室一览**：一个聊天窗口对应一个独立档案；列表只扫描当前角色的聊天窗口。
- 主内容入口为五个：**回忆相簿 → ADV EVENT → 他的房间 → ENDING / 后日谈 → 蝴蝶效应**。
- “他的物品”和“他的手机/私人终端”是“他的房间”内部的深层访问，不再作为档案室头像入口。
- 未生成时不会因为误点头像自动发请求；每张主入口卡片都有自己的“生成这一项”按钮。已经生成的入口可以继续点击头像查看；档案增加后再点“增量追加”，旧内容不会替换。

## 独立生成与并行任务

档案室五个主入口分别请求各自的数据：

- 回忆相簿
- ADV EVENT 事件索引
- 他的房间
- 蝴蝶效应
- ENDING / 后日谈

可以连续点击多个入口，最多 5 项同时后台生成。某一项失败不会让其他入口一起失败，也不会删除之前已经成功保存的内容。

“他的物品”从房间实际收纳物进入；r52 起“他的私人终端”从档案室独立入口进入，不要求先生成房间。未完成终端草稿可继续生成。

有两类内容仍然按需单独请求，这是有意设计：

- **长篇 ADV 正文**：用户点开具体 CG 事件时才生成，生成后缓存。
- **他的房间 · 今日生活时间线**：每天属于短期动态状态，不和长期房间蓝图绑死；当天生成后整天只由本地时钟切换节点。

长聊天的“创建/更新档案”仍可能分块请求，因为它需要把历史文本分段整理，不能安全地把超大聊天一次塞进上下文。

0.8.6 不再只截取最近 36 万字符。普通长聊天优先覆盖整个窗口；当前硬上限为 4000 条可用消息 / 约 120 万字符。只有超过这个级别时才会从**整个窗口均匀取样**，而不是只保留近期。分块抽取还会保留真实消息编号，并把不同 messageRange 的相似标题视为不同事件，避免远隔剧情被误去重。档案最多保存 240 条记忆；生成玩法时从整份档案均匀抽取最多 64 条作为模型上下文。

## 真正后台生成

模型生成时不再锁死在 loading 页面。

加载页提供：

- **返回档案室 · 后台继续**
- **关闭 · 后台继续**

关闭后可以继续正常使用 SillyTavern。请求仍然运行，完成后只把结果写回发起任务时绑定的 `chatId + archiveRevision` 缓存，并通过 toast 提示完成。若期间切换聊天或更新档案，迟到结果会被拒绝，不会串档。

重新打开档案室时，如果仍有任务进行，会看到轻量后台任务提示；设置页不会再反复渲染档案卡和玩法按钮。已有缓存仍可浏览，但会阻止再启动第二个模型请求。

## 角色 / 用户 / 世界书上下文

生成时使用受控上下文：

- 当前 `{{char}}` 的角色卡字段（description / personality / scenario / depth prompt / creator notes 等）；
- 当前 `{{user}}` 名称与 SillyTavern Persona description；
- 由“角色设定 + User Persona + 最后一次手动档案”进行 dry-run 后激活的相关世界书；
- 最后一次用户手动创建/更新的聊天档案。

角色卡、Persona 和世界书只负责设定一致性；**过去是否真的发生过**仍然只认手动聊天档案。未归档的新聊天不会因为 Persona / 世界书读取而被偷偷送进生成请求。

## 当前聊天窗口记忆补充

0.8.6 增加“当前窗口记忆桥接”。它只在用户手动点击 **创建聊天档案 / 更新聊天档案** 时工作，不参与 CG、ADV、房间或蝴蝶效应的日常读取。

- 数据边界锁定为当前 `chatId`。切换聊天时正在读取/整理的任务会中止，结果不会写入其他聊天。
- **SillyTavern Memory / Summary**：如果当前聊天存在 `1_memory` 注入的总结，会作为当前窗口的补充资料读取。
- **已注册公开来源**：当前只有 SillyTavern `1_memory` 与柏宝书 Public API v1。柏宝书必须显式声明 `apiVersion === 1`，并返回与当前窗口一致的 chat / revision / coverage；不一致时整份拒绝。
- **r49 已退役旧兼容路径**：不再读取 EverMind 私有 settings / metadata / API Key，也不再扫描全局对象、脚本路径或猜测 reader 方法名。未注册插件只能通过用户明确选择的惰性 JSON / JSONL / TXT / Markdown 导入。
- 心跳回忆不会去扒别的插件私有数据库、DOM、设置、凭据或内部变量，也不会根据来源返回的数据执行任意 URL。公开接口返回值一律只按不可信文本资料处理。
- 外部记忆只在手动建档/更新时同步进心跳回忆自己的 M001... 档案。之后相簿 / ADV / 房间 / 蝴蝶效应仍只读心跳回忆档案，不会每次重新请求外部记忆服务。
- 外部记忆预读取最多接收 256 条 / 约 240,000 字符，并按约 26k 字符分块整理；不会把整库一次塞进模型。每条外部候选记忆都必须返回真实 provider record ID，并通过逐字 `sourceExternalAnchor` 校验后才可进入档案。

档案室提供开关“创建/更新档案时补充读取当前聊天窗口的记忆插件档案”。默认开启；关闭后只扫描聊天正文。

## 升级兼容原则

心跳回忆把“插件版本”和“档案数据 schema”分开管理。升级 0.8.x 不会因为 manifest 版本变化自动删除 `heartbeatMemoriesArchiveV3` 或已生成剧场缓存。未来如果数据结构真的需要升级，必须提供显式迁移；不会把“版本号不一致”当成清空旧档案的理由。

需要区分：用户主动点击“更新聊天档案”会产生新的 archive revision。旧相簿/ADV/房间等如果仍引用旧 revision，会被主动失效，避免把旧证据和新档案混用。这是手动更新档案的行为，不是插件升级行为。

## 聊天档案

每个聊天窗口各自拥有独立档案：

- 第一次由用户手动“创建聊天档案”。
- 后续聊天不会自动进入档案。
- 只有用户主动点“更新档案”，新增聊天才会被重新整理。
- 所有声称“已经发生过”的 ADV EVENT / 回忆 / 与用户有关的房间痕迹，都必须引用真实 `sourceMemoryIds` 并通过 `sourceMemoryAnchor` 证据校验。

### 档案室一览

档案室通过 SillyTavern 同源的 `/api/characters/chats` 以 `simple:true` 只读取**当前角色**的 chat ID / 文件名目录，不再自动扫描所有聊天 metadata。当前聊天自己的档案摘要直接读取当前窗口 metadata；点击一篇非当前历史档案时，使用 `/api/chats/get` 只请求该目标聊天并从 header 提取 Heartbeat metadata，不再读取同角色全部聊天 metadata。SillyTavern 当前没有单篇 metadata-only 接口，因此目标聊天本身仍会完整传输/解析，但不会切换宿主聊天，也不会把正文写进 Heartbeat snapshot、Prompt 或 DOM。列表按角色缓存约 60 秒，也可手动刷新；后台生成期间禁止切换聊天，避免异步回写串档。

## 他的房间：现实时间流动

房间分为长期空间蓝图与短期“今日生活时间线”。每日生活生成 8～14 个节点，之后本地时钟在已有节点间切换，不会每 30 秒请求 API。

r43.0 起，房间外观与房中 CSS 人物会共同采用人物视觉档案：世界书/角色设定的明确描述优先，缺少时按时代、身份、职业、生活方式和气质选择代码内固定枚举。r44.0 进一步固定为侧后/背影视角，删除正脸几何，并用职业兴趣母题和物件种类拉开不同空间的结构；人物始终是与 CSS 场景一致的抽象轮廓，不嵌入档案头像。角色明确养有宠物时，宠物会出现在对应空间；没有明确依据时不会发明。

时间节点可以改变角色所在空间、活动、短台词、光线、窗帘/窗户、整洁程度、桌面用途和少量当天临时物件。与 {{user}} 有关的旧痕迹仍然只能来自手动档案。

如果当天自动生成失败，会落地本地 fallback 并熔断当天自动重试，只有用户手动“更新今日生活”才会再次请求。

房间侧栏新增 **PRIVATE ACCESS**：

- “翻找这里的物品 / 收纳”会进入与当前空间尽量匹配的储物容器。容器不限抽屉，可按世界观使用衣柜、箱笼、行李箱、船舱格、实验室柜、古代匣盒、袖袋、幻想/科幻收纳等，并允许 1～3 层子容器。
- “查看私人通讯终端”在现代可表现为手机；非现代世界会适配为腕式设备、私人终端或传讯器。进入后先显示完整设备主页，点 App 进入列表，再点具体条目进入详情；主题、壁纸、图标和 App 组合随人物与设备变化。地图、导航、路线与出行不再作为终端 App，它们已迁到档案室独立的“他的出行路线”。
- 两者都有“返回他的房间”，不会作为档案室独立入口，也不会因此多发一次基础 API 请求。

## 他的出行路线：近地图对白与远地图明信片

“他的出行路线”是档案室独立入口。模型只提供有证据的地点文字、对白和明信片文案；地图坐标、连线、图标、配色、明信片版式和全部交互均由插件本地固定代码控制。附近地点至少有三句可逐页播放的 {{char}} 对话；远方地点会按海港、雪山、森林、城市等白名单主题绘制本地 SVG/CSS 风景，再叠加明信片文字，不请求或嵌入图片 URL。

## 蝴蝶效应：平行时空观测终端

页面采用垂直双层终端结构，依次包含 Terminal Header、上半区常驻 Divergence Map、下半区 Observation Screen、Reality Intervention 与最底部 dashed System Note。分歧树固定包含锁定主时间线、至少 8 个由角色卡 / User Persona / 相关世界书推演的外延节点，以及末端不同频率发光的 TRUE ENDING / 观测点 Ω。

r43.0 冻结这套界面与动效，只调整内容生成和本地验收；不会把它改成另一套页面，也不会改变既有配色、分歧树、SIGNAL 动画或 Ω 的位置。

点击任一外延节点时，本地先播放 **1 秒 SIGNAL INTERFERENCE / LOADING TEMPORAL DATA**，再切换观测内容。每个平行体独白至少 100 个汉字；现世介入由当前世界的 `{{char}}` 实时自省/告白；系统评价使用冷酷中文算法口吻。外延世界是明确的模拟切片，不要求伪造 `sourceMemoryIds`，也不会写回档案。主时间线仍必须由真实档案锚定。任何前任/前女友，以及 `{{char}}` 与 `{{user}}` 之外的恋爱、婚姻或组建家庭情节仍被禁止。

## Token 与请求频率

“最大输出”是单次输出上限，可在设置页设到 **60,000 tokens**。保留用户自己选择的较低值；插件不会因为升级自动把既有设置抬到 60k。0.8.9.1 起每个入口独立请求，因此不会再为了整套基础包一次性要求超长输出；代价是同时生成多个入口时会分别携带各自需要的上下文。

普通生成最多抽取 64 条均匀覆盖时间线的档案记忆；最终发送前还有约 **32,000 input tokens / 96,000 字符**硬预算，超限直接停止，不发送给模型。60k 是“插件允许请求的输出上限”，不是模型能力保证；模型自身上下文/服务端上限更低时仍可能由 provider 拒绝。

r20 起，创建/更新档案时聊天分块与记忆/摘要分块不再硬限制 4096 输出 tokens，而是使用用户“最大输出”。若模型只产生 reasoning 却没有 final JSON、返回普通文本、JSON 被截断或格式错误，插件会给出不同提示；档案分块只会在你明确确认后额外重试当前这一块一次，不会自动烧第二次请求，也不会先覆盖旧档案。

## 安装

推荐在 SillyTavern 第三方扩展安装界面直接输入仓库地址：

`https://github.com/Zaiyebuzuoyouqingdetiangou/tokimemo`

仓库根目录直接包含 `manifest.json`、`index.js` 与 `src/`，适合作为第三方扩展 Git 源安装和更新。

最低客户端版本：**1.18.0**。

## 安全

见 `SECURITY.md` 与 `SECURITY_REVIEW.md`。

## License / 授权

本项目 **不是开源软件**。版权所有，详见根目录 `LICENSE`（《心跳回忆有限个人使用许可证 Version 1.1》）。

测试者仅被授权安装并运行未修改版本用于个人、非商业测试；不得复用源代码、制作衍生项目、修改后发布、镜像、再分发或商业使用。若希望测试阶段连源码本身也不公开，请将 GitHub 仓库设为 Private；公开仓库即使采用专有许可证，仍无法从技术上阻止他人查看或下载源码。

## 共同回忆与房间深层玩法

- 相簿中每个已解锁 CG 的“共同回忆”不再只有 3 句短评；模型必须生成 **6～8 段**角色回想，覆盖当时注意到的细节、没说出口的念头、对 User 的观察、情绪转折、事后理解和现在回看时的感受。
- 房间内的“翻找物品”不是固定现代抽屉 UI。模型可以按时代/职业/居住环境生成衣柜、床头柜、箱笼、行李箱、旅行袋、工具箱、药箱、首饰盒、储物柜、古代匣盒、袖袋、船舱储物格、实验室柜、幻想/科幻收纳结构等；容器可以再打开 1～3 层子容器。
- 房间和人物使用同一份安全的人设视觉档案，但不显示档案头像；外观只能从代码内置的房型、主题、服装、发型与轮廓枚举中组合。
- 房间内的“私人终端”在现代设定表现为私人手机；非现代世界可适配为腕式设备、私人终端或传讯器。主页 → App 列表 → 条目详情是三个明确层级，App 名称、类型、数量、顺序与主题随人物变化；不复制真实商业 App UI。双方日期、约定和纪念日不在终端重复生成，统一由独立「两个人的日历」承担。
- 物品/手机中凡明确声称“User 与 Char 过去共同发生过”的内容，仍必须经过 `sourceMemoryIds + sourceMemoryAnchor` 校验；纯角色设定推导会标记为“生活设定”，不能伪装成共同历史。

## 0.8.8 档案室与后台任务

- 打开“档案室”先进入**全角色档案库**：只显示已经建立过心跳回忆档案的角色；点进角色后再按聊天窗口查看各自独立的档案名称。
- 新档案会自动写入轻量全局索引；旧版本档案可手动点“扫描旧版本已有档案”迁移。该重扫描不会在日常切聊天时自动执行。
- 建档前增加**扫描记忆 / 摘要**步骤：被动读取当前提示注入摘要与当前聊天 metadata 摘要；只有用户另外开启第三方公开 reader 授权时，才会调用 `getInjectedHistory()` / 其它 current-chat reader / `getSnapshot()`，并显示实际读取来源、条数和字符量，再允许创建/更新档案。
- `getSnapshot()` 与 `getInjectedHistory()` 会合并读取；外部记忆扩大到最多 256 条 / 24 万字符并分块整理，避免只吃到当前注入的少量记忆。
- 生成任务不再因为切换聊天窗口而主动 abort。结果始终绑定发起时的角色、chatId 和 archiveRevision；若完成时原窗口不在前台，会暂存并在回到原窗口后自动写回。


## 0.8.10 r10

重新生成与当前窗口档案创建/更新均增加明确确认；设置面板重新提供“生成/更新当前窗口档案”直达入口。


## 记忆相关世界书（r19）

“扫描记忆 / 摘要”旁新增 **选择记忆相关世界书**。这是给“记忆插件的摘要/条目需要另一本文档解释人名、地点、组织、术语或关系背景”的情况用的。

- 可以勾选某本世界书 **整本导入**。
- 也可以展开世界书，精确勾选具体 UID 条目。
- 选择按**当前聊天**保存在 chat metadata 中，只保存世界书名称、整本/精确模式和 UID 列表，不复制世界书全文，也不修改世界书。
- 真正读取只发生在用户显式点击“扫描记忆 / 摘要”时，通过 SillyTavern 公开 `getWorldInfoNames()` / `loadWorldInfo()` 读取。
- 这些世界书内容只会与记忆/摘要一起作为**解释上下文**发送：它能帮助模型理解摘要里的“他/那个地方/某组织”指什么，但**不能单独制造已经发生的回忆，也不能充当 sourceExternalId / sourceExternalAnchor**。事实仍必须锚定当前聊天记忆/摘要本身。
- 普通未选择的世界书继续只是设定上下文。整本/精确导入总计最多 8 本、160 条、52,000 字符，避免挤爆外部记忆请求。

如果你只选择了记忆世界书、但当前没有任何可读的记忆/摘要，世界书不会单独生成档案事实；它只是等待以后配合真正的记忆/摘要使用。

## 当前窗口记忆 / 摘要兼容（r11 历史行为）

> 历史说明：以下三路是 r11 当时的兼容策略。r49 已删除第 1 项的未知全局方法猜测以及第 2、3 项的任意 prompt / metadata 启发式扫描；当前生产版本只保留精确 `1_memory`、柏宝书 Public API v1 和用户显式文件导入。

当时建档会按顺序尝试：

1. 能明确表示“当前聊天”的公开 API（`getInjectedHistory`、`getCurrentChatMemories`、`getCurrentChatMemory`、`getCurrentChatSummary`、`getCurrentSummary`）；
2. 当前 SillyTavern prompt 中明确标为 memory / summary / 摘要 / 总结的注入文本；
3. 当前聊天 `chatMetadata` 中明确标为摘要/记忆的数据。

所有补充资料都要经过 externalId + 原文 anchor 校验。世界书、角色卡、作者注记属于设定上下文，不作为“某件事已经发生”的证据；原始当前聊天正文仍是最可靠的事实来源。

### r13: incremental archives and read-only archive browsing

- “更新当前窗口档案” is incremental by default: it only processes chat messages added after the previous archive plus changed current-chat memory/summary sources. Existing Mxxx IDs and generated Album/ADV EVENT/Room/Ending/Items/Phone content are retained.
- If an already archived message was edited/deleted/reordered, the incremental update stops. Use the separate “完全重建档案” only when you intentionally want a full rescan and are willing to invalidate derived content.
- ADV bulk generation no longer auto-retries every failure individually. After a partial failure, choose either one more bulk request or an explicitly confirmed per-item repair pass.
- Opening another character/chat archive from the archive library is read-only and does not switch the SillyTavern chat. Generated content can be viewed from saved metadata; mutation/generation actions remain disabled until you intentionally return to that chat.
