# RabbitMirror 0.31.89

正式结合版：保留 7.10 审美、完整主题/展现形式库、默认交互、代码块急救与智能交互急救。

本版只新增两项：

- 动态视觉模式开启时，要求实际生成无需用户触发、持续运行且肉眼可见的自动动态；仅有 transition 不算完成。
- 智能交互急救可保守解析 checkbox/radio 的多目标 onchange 样式切换，不执行模型生成的 JavaScript。

详细变更见 `README-0.31.89.md`。
