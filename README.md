## v0.31.25-710-mirror-r4-pure-dedupe

- 仅删除每轮 Prompt 中的纯重复内容，不压缩或删除审美、交互、中文锁定、盒模型与安全规则。
- 删除重复的「本轮抽取模式」说明；主题元素与展现形式本身仍完整保留。
- 删除末尾与「强制输出」重复的「最终保底」。
- 没有实际历史与风险时，不再注入空的「近期视觉避让」提醒。
- 删除每个抽取条目后反复出现、且已由全局核心规则覆盖的执行尾注。
- 其余生成逻辑、抽取库、视觉指纹、内部交互、语言锁定与代码块急救均未改动。

## v0.31.25-710-mirror-r3-langlock

- 将 `runtimeLanguageRules.js` 正式接入每轮 Prompt，不再只是存在于文件中。
- 所有生成界面可见文字默认使用简体中文；确需外语时，必须逐词、逐句紧跟【简体中文释义】。
- 外语学习类界面仍可显示外语，但按钮、菜单、功能名和操作提示保持简体中文。
- 删除抽取条目中对模型无必要的英文 `tags` 注入，减少外语诱导并抵消语言规则的 token 增量。
- 保留「兔子镜」命名、视觉指纹、H.6、自由交互和原有 UI 审美规则。

## v0.31.25-710-mirror-r2

- 六项视觉指纹只在实际运行的 `src/promptBuilder.js` 中保留一次；旧规则文件中的重复段已删除。
- 所有中文可见文案、点播关键词与历史识别提示统一使用「兔子镜」；内部英文函数名和 `data-rabbit-hole` 标记保持不变，避免破坏插件结构。
- 新增 H.6「事后对账 / 真相拼图」。
- 删除复杂度规则中 grid、flex-wrap、分栏、网格、时间轴等具体布局示例，改为抽象的空间组织、阅读路径与层级关系要求。
- 删除 Prompt 中具体的卡片/报告/面板式禁令，保留对同构内容、浅层承载、近似骨架和换皮复用的抽象判断。
- 视觉变化规则合并为六项指纹：layout / material / color / type / interaction / hierarchy；与上一轮至少四项变化。
- 优化「10.1 可交互小剧场」：不设固定交互池，要求内部真实交互改变可见层级、阅读路径或界面状态，外层折叠本身不计。
