## 0.32.59 TEST 按钮后置内容计算样式识别急救

- 基于 0.32.58 TEST，完整保留逐镜 CSS/class 隔离、完整交互链规则、智能交互生成时序隔离、原版推荐正则及全部既有功能。
- 修复按钮后置结果的隐藏状态只写在 `<style>` 中时，扫描器仅查看 inline style，导致 `opacity: 0`／`display: none`／`visibility: hidden` 或折叠高度未被识别的问题。
- “按钮＋后置隐藏内容”路线现在读取当前节点的计算样式；点击一次以重要级内联状态揭示内容，第二次点击恢复原始样式，并主动移除按钮焦点，避免原始 `:focus + ...` 规则阻止再次隐藏。
- 计算样式中的位移或缩放隐藏态会在揭示时中和；诊断器同步将该目标计入 `hiddenCandidates` 和“按钮后置内容”命中统计。
- 不修改 Prompt、抽选池、配色审美、共同回忆逻辑、聊天原文、swipe 或 `display_text`。

## 0.32.58 TEST 逐镜 CSS／class 隔离急救

- 基于 0.32.57 TEST，完整保留完整交互链规则、智能交互生成时序隔离、原版推荐正则、CSS 注释剥离、类名状态程序、CSS 声明保全及全部既有功能。
- 修复单条纯文字急救或代码块急救后，不同消息中重复使用 `.rabbit-mirror-container`、`.main-title`、`.text-content` 等通用 class，导致旧兔子镜的固定高度、黑色背景、`overflow`、边框或阴影串入当前作品的问题。
- 每次实际执行上述急救时，为当前兔子镜生成独立的 `data-rabbit-mirror-css-scope`，并仅将本地 `<style>` 中实际使用的 class 改成逐镜唯一名称；外部图标等未被本地 CSS 使用的 class 保持原样。
- 普通选择器、逗号选择器、`:hover`、`:active`、`:checked`、伪元素及 `@media`／`@supports` 内规则均加入当前兔子镜作用域；`@keyframes` 的 `from`、`to` 和百分比帧不作选择器改写。
- 智能交互急救的安全 class 查询增加逐镜 class 后缀识别，原有按钮、状态类及安全查询交互继续兼容。
- 不开代码块急救、且未对某条消息执行单条纯文字急救时，不改写正常输出；不修改 Prompt、抽选池、配色审美、聊天原文、swipe 或 `display_text`。

## 0.32.57 TEST 完整交互链规则

- 基于 0.32.56 TEST，完整保留智能交互生成时序隔离、原版推荐正则、CSS 注释剥离、类名状态程序、CSS 声明保全、共同回忆资料来源试作及全部既有功能。
- 将复杂交互核心中的“至少一个交互变化”改为“至少一组完整交互链”，明确要求可触发对象、可保持或可识别的状态变化，以及触发后的第二层体验实际存在。
- 第二状态允许在内容、结构、空间关系、视觉层级、材质表现、时间进程或观察视角中发生变化，避免交互长期退化为单一的显隐或翻面。
- 合并 Visual Scenery 中两条重复交互要求，继续排除仅改变颜色、边框或阴影的伪交互。
- 除上述两处交互规则外，其他 Prompt 内容、抽选池、功能逻辑、配色与审美均未修改。

## 0.32.56 TEST 智能交互生成时序隔离

- 基于 0.32.55 TEST，完整保留原版推荐正则、CSS 注释剥离、类名状态程序、CSS 声明保全、折叠设置页、共同回忆资料来源试作及全部既有功能。
- 智能交互急救新增生成状态边界：从 `GENERATION_AFTER_COMMANDS / GENERATION_STARTED / STREAM_TOKEN_RECEIVED` 起暂停自动交互处理，只在 `GENERATION_STOPPED / GENERATION_ENDED` 后对已稳定渲染的兔子镜执行。
- `MESSAGE_RECEIVED` 阶段不再启动智能交互急救；等待 `CHARACTER_MESSAGE_RENDERED` 或生成结束事件，避免与 Reasoning、显示正则及消息最终渲染发生时序竞争。
- 移除对聊天正文的常驻 `MutationObserver` 扫描；若初始化时聊天根节点尚未挂载，仅用一次性观察器等待 `#chat` 出现，随后立即断开。RabbitMirror 自己追加 style、改写 ID 或节点的动作不再反向触发新一轮自动急救。
- 生成结束后保留多次有界延迟扫描，以兼容宿主较晚挂载的样式和交互节点；新调度会取消旧批次，不形成无限扫描回路。
- 损坏 SVG data URI 保全也被限制到生成结束后的稳定阶段。
- 不修改 Prompt、不增加生成 token、不改配色、审美、抽选池、共同回忆逻辑、聊天原文或推荐正则。

## 0.32.55 TEST 推荐正则原版恢复

- 基于 0.32.54 TEST，完整保留 CSS 注释剥离急救、类名状态程序、CSS 声明级保全、折叠设置页、共同回忆资料来源试作及全部既有功能。
- 设置页“复制推荐正则”恢复为原版：`/<toto\b[^>]*>[\s\S]*?<\/toto>\s*/gi`。
- 不再在推荐正则中额外匹配 Markdown 代码围栏；代码围栏损坏仍由原有代码块急救按开关处理。
- 不修改 Prompt、不增加生成 token、不改抽选池、配色、审美、共同回忆逻辑或聊天内容。

## 0.32.54 TEST CSS 注释剥离急救

- 基于 0.32.53 TEST，完整保留类名状态程序、已渲染 CSS 注释边界保全、CSS 声明级保全、折叠设置页、共同回忆资料来源试作及全部既有功能。
- 修复完整合法的 `/* ... */` CSS 注释在 SillyTavern／Markdown 渲染链中被破坏后，宿主误报 `CSS ERROR: ... missing '}'`、兔子镜退化成纯文字的问题。
- 代码块急救或单条纯文字急救实际整理 `<style>...</style>` 时，删除其中引号外的 CSS 注释，并保留一个安全空格，避免删除注释后相邻 token 意外粘连。
- 字符串内容中的 `/* ... */` 原样保留；遇到未闭合 CSS 注释时丢弃其后注释内容，优先保住此前完整规则。
- 不开对应急救时完全不介入；不修改 Prompt、不增加生成 token、不调整配色或审美、不改抽选池及共同回忆逻辑。

## 0.32.53 TEST 类名状态程序＋CSS 注释保全急救

- 基于 0.32.52 TEST，完整保留 CSS 声明级保全、折叠设置页、共同回忆资料来源试作、严格交互扫描、五轮配色冷却及全部既有功能。
- 新增安全类名状态程序回读：当宿主删除 `onclick` 后，识别固定字符串形式的 `document.getElementById(...).className='基础类 状态类'`，在当前兔子镜内重新绑定按钮状态切换。
- 自动映射酒馆生成的 `custom-` class 前缀；切换状态时保留当前根容器的结构类，只移除急救器上一轮加入的状态类，支持播放／快进／停止这类互斥状态。
- 新增 CSS 注释边界保全：识别 Markdown 把相邻 `*/ ... /*` 错误解析成 `<em>/ ... /</em>` 的明确损坏形态，在当前已渲染 DOM 中恢复注释边界，避免中间状态规则整段失效。
- 两项修复均接入智能交互急救路径，不单独常驻；只修改当前渲染 DOM，不改写或保存聊天原文、swipe、`display_text`。
- 不修改 Prompt，不增加生成 token，不调整配色或审美，不过滤主题与展现形式，也不恢复单兔子镜限制。
- 一次性交互诊断新增“类名状态程序”与“CSS注释保全”命中统计。

## 0.32.52 TEST CSS 声明级保全急救

- 基于 0.32.51 TEST，保留折叠设置页、共同回忆资料来源试作、严格交互扫描、五轮配色冷却及全部既有急救。
- 新增精准 CSS 声明修复：识别 `transform: ... filter(...);` 这类把滤镜误塞进 transform 的模型输出，自动拆为独立的 `transform` 与 `filter` 声明。
- 典型修复：`transform: scale(1.1) filter(blur(4px));` → `transform: scale(1.1); filter: blur(4px);`。
- 若该条 `filter(` 外层括号本身无法闭合，无法安全拆分时只删除这一条损坏的 transform 声明，保留其余 CSS、HTML、文字和交互。
- 修复接入单条纯文字急救与代码块急救共用的样式整理链；不开对应急救时不干预正常消息。
- 不修改 Prompt、抽选池、资料来源读取逻辑或聊天原文。

## 0.32.51 TEST 设置界面分组折叠版

- 基于 0.32.50 TEST，只调整设置页展示结构，不修改 Prompt、抽选、共同回忆读取、急救或扫描逻辑。
- 「兔子镜自动注入」保留在最外层，打开设置后可直接查看与切换。
- 其余功能按「生成设置」「共同回忆资料来源」「急救与诊断」「工具与维护」分为四个折叠区，默认收起。
- 共同回忆扫描结果、急救按钮、正则与维护操作仅在展开对应区域时占用页面高度。
- 使用原生 details/summary，不增加运行监听，也不影响各控件原有 ID、设置保存与事件绑定。

## 0.32.50 TEST 共同回忆资料来源界面试作版

- 基于 0.32.49 TEST，仅调整共同回忆资料来源区域的名称、说明与展示层级。
- 设置页不再使用“记忆插件”“外部记忆读取”等窄范围称呼，统一改为“共同回忆资料来源”“额外资料来源”“扫描可用资料来源”。
- 扫描结果顶部固定显示“当前模型上下文 [已可用]”，说明近期对话、已注入世界书及模型当前已有摘要/总结无需重复读取。
- 公开接口读取结果作为“额外资料来源”单独列出；其他候选继续默认折叠。
- Prompt 仅把测试标记从“记忆扫描测试版”改为“资料来源测试版”，没有增加新的常驻规则。
- 扫描、读取、I.1 限定注入、0.32.47 全部急救、严格交互扫描与五轮配色冷却保持不变。

## 0.32.49 TEST 记忆扫描收窄版

- 基于 0.32.48 TEST，保留共同回忆来源读取、0.32.47 全部急救、严格交互扫描与五轮配色冷却。
- 扫描器不再把整个 `globalThis` 上带有泛化关键词的浏览器对象列为候选；只有实际提供可调用 `getInjectedHistory()` 的公开来源进入可读取主列表。
- `AudioContext`、`BaseAudioContext`、`CanvasRenderingContext2D` 等浏览器原生对象不再出现。
- 来源名称优先读取插件公开接口或扩展加载信息中实际声明的名称；没有声明时才显示公开全局对象名，不额外猜测插件名称。
- 主列表只展示可读取来源；待适配候选改为默认收起，最多展开显示 10 项，避免占满手机设置页。
- 移除扩展设置页面 DOM 的泛扫描，避免普通按钮、设置项和其他无关控件被误判为记忆插件。
- 设置页不加入合作、隶属或官方兼容等额外说明，只客观显示扫描与读取状态。
- 兼容 0.32.48 已保存的来源勾选记录；Prompt、主题池、展现形式池和记忆注入上限未改。

## 0.32.48 TEST 共同回忆插件扫描测试版

- 基于 0.32.47，保留展开透明度保全、严格交互扫描、五轮配色冷却、损坏 data URI 保全与全部既有功能。
- 设置页新增明确标注的「共同回忆插件扫描 TEST / 测试版」区域，默认关闭。
- 点击扫描后，会从公开全局 API、扩展设置记录和已加载脚本中寻找疑似记忆插件；扫描到不等于可读取。
- 已适配来源或公开提供无参数 `getInjectedHistory()` 的来源可勾选并执行「测试读取」；未知插件只显示「待适配」，不会翻查其内部变量、数据库或聊天元数据。
- 当前测试读取器使用来源公开提供的 `getInjectedHistory()`；若来源同时提供快照接口，则显示当前角色、聊天标识与覆盖缺口。
- 只有抽中 `I.1 共同回忆` 且总开关开启时，才读取已勾选来源并把资料加入该轮 Prompt；其他 97 个主题不读取，也不追加记忆正文。
- 外部记忆总长度默认最多 2200 字符；多来源会去重并统一截断。
- `I.1` 文案改为来源无关：可使用当前可见对话、已启用世界书和本轮附带的共同回忆资料，资料不足时不得补造过去。
- 测试版关闭自动更新，界面、版本号与诊断报告均带 TEST 标记。

## 0.32.47 展开后残留透明度保全急救

- 基于 0.32.46，保留严格有效交互扫描、五轮配色冷却、损坏 data URI 保全、focus→checked 精准急救、169 条展现形式、98 条主题元素及全部既有功能。
- 仅在智能交互急救开启后处理：checkbox/radio 已激活、真实 `:checked` 规则已让目标展开、目标具有实际内容与自然高度、计算样式仍为 `opacity:0`，且激活规则未明确声明 opacity 时，临时补 `opacity:1 !important`。
- 取消勾选后精确恢复原有 opacity；不会保存或改写聊天原文，也不会处理主动要求保持透明的状态。
- 诊断新增“展开透明保全”命中统计。
- Prompt、主题池、展现形式池与配色冷却规则均未改动。

## 0.32.46 严格交互扫描 + 五轮配色冷却

- 基于 0.32.45，保留损坏 data URI 保全、focus→checked 精准急救、169 条展现形式、98 条主题元素、ADV/Adv Event、最底部位置锁与全部既有功能。
- 严格交互扫描：`:hover`、`:active`、`transition`、`cursor:pointer`、按钮外观和操作文案不再单独算有效交互。
- 有效交互需存在可验证的第二状态机制，例如内部 details、checkbox/radio + 有意义的 `:checked` 状态、`:target` 或原生 popover。
- 只有悬停、位移、变色或装饰性入口时记录 `fake_interaction`，下一轮触发针对性纠偏。
- 启用实际配色指纹：扫描结果会把 brightness、综合色相、饱和度、冷暖、主承载明度比例与置信度写入既有历史记录。
- 任意一轮检测到高置信度低明度主承载关系后，后续 5 轮自动进入配色冷却；冷却期再次命中会重新计算 5 轮。
- 常驻展现形式规则已删弱反复提示特定暗色方案的措辞；配色冷却只在真实输出命中后临时注入。
- 不自动改写已生成 CSS，不修改聊天原文，不过滤任何主题或展现形式。

## 0.32.45 损坏 data URI 保全急救

- 基于 0.32.44，保留 169 条展现形式、98 条主题元素、ADV/Adv Event、展现形式落地、色彩组织、最底部位置锁与全部既有急救。
- 新增保全型 data URI 急救：仅在智能交互急救开启时，识别会提前截断 inline style 属性的损坏 SVG data URI。
- 命中后只移除损坏的 `background`／`background-image` data URI 声明，保留该条兔子镜其余 HTML、CSS、文字与交互结构。
- 健康的百分号编码或 base64 data URI 不处理；普通背景、渐变、配色与其他资源不修改。
- 使用临时消息副本重建当前 DOM，不改写 `chat[].mes`、swipe 或 `extra.display_text`，也不保存聊天原文。
- 本次未修改 `src/promptBuilder.js` 或任何 Prompt 数据文件，不增加 Prompt，也没有新增禁止规则。

## 0.32.44 checkbox/radio focus→checked 精准急救

- 基于 0.32.43，完整保留 169 条展现形式、98 条主题元素、ADV/Adv Event、展现形式落地、色彩组织与最底部位置锁。
- 新增精准交互急救：当 checkbox/radio 的持久状态被误写为 `:focus ~ ...` 或 `:focus + ...` 时，只在当前兔子镜内复制为该控件唯一 ID 的 `:checked` 状态规则。
- 兼容酒馆把隐藏 input 自动包进 `<p>`、导致原兄弟选择器失配的情况；现有 checked 文本兜底会在当前兔子镜根内落实对应目标，并在取消勾选时恢复。
- 不改写原始消息，不保存聊天，不处理其他兔子镜，也不触碰普通按钮、链接、文本输入框的 focus 样式。
- 一次性交互诊断新增 `focus→checked` 命中统计。
- `src/promptBuilder.js`、主题库、展现形式库和全部 Prompt 数据未修改，因此本次不增加 Prompt。

## 0.32.43 原始主题／展现形式全量抽取池

- 基于 0.32.42，保留展现形式落地、色彩组织、最底部位置锁、单条纯文字急救与现有交互修复。
- 展现形式抽取池由 93 条恢复为原始库全部 169 条；主题元素抽取池由 83 条恢复为原始库全部 98 条。父级／综合条目与具体子项均进入候选池。
- 不改写原始库标题与描述；结构化池直接使用原始条目的标题、描述与原始行。
- 删除报告／档案类展现形式的默认随机过滤；身体状态报告、心跳回忆 GS·多重结局系统、角色扮演游戏、恐怖／解谜游戏均可正常随机抽取。
- 删除 G.3.5／G.3.11 地区主题排除逻辑；主题池不再存在内容级隐藏过滤。
- 取消富版式候选三倍加权，所有展现形式在进入近期冷却前以相同基础权重参与随机。
- ADV 相关形式完整保留：6.2 视觉小说与养成、6.2.1 视觉小说／Galgame／乙女游戏，以及 6.2.1.1.c Adv Event。

## 0.32.42 展现形式落地＋色彩组织

- 基于 0.32.41，保留“正文／状态栏等固定模块 → 兔子镜 → 结束回复”的最底部位置锁。
- 旧的“展现形式优先配色”整段已被替换，不再与新规则叠加。
- 新增“展现形式落地”：首个主要内容块必须直接构成媒介本体，外层容器只负责显示边界；禁止先搭通用圆角面板、卡片列表、仪表盘或信息框再填内容。
- 明确平面媒介例外：纸面、印刷面、画布、版式、纹理、边缘与承载内容可直接成为主要视觉，不会误伤格子纸、信件、小说页、日记、报刊等形式。
- 所有可见颜色必须配合本轮展现形式的材质、环境与光线，标题和情绪词不能单独触发深色面板、警报界面或科技仪表盘。
- 新增“色彩组织”：建立主次、明度／饱和度／冷暖／透明度／材质层级，限制强调色面积，避免机械填色与渐变、发光、阴影、高饱和度堆叠。
- 文字数量、密度与排版由展现形式决定，文字媒介可以让正文与版式成为主体。
- 不增加清洗器自动改色，不在运行时重写正常兔子镜。

## 0.32.41 兔子镜最底部位置锁恢复

- 保持 0.32.40 已撤回的“全回复只能出现一个兔子镜”专项单镜锁，不恢复副本、预演、唯一版本等防重复判断。
- 恢复独立的输出位置约束：先完成正文、状态栏、变量栏及其他固定模块，再输出兔子镜。
- 兔子镜必须位于整条回复最底部并作为最后一个可见模块；完整输出 `</toto>` 后立即结束，不得再追加状态栏或其他内容。
- 0.32.40 的输出长度预留、局部 checked 交互急救及其余运行逻辑保持不变。

## 0.32.40 输出预留与局部 checked 交互修复

- 撤回 0.32.33 加入的“全回复只能出现一个兔子镜”专项单镜锁，不再要求模型处理副本、预演、唯一版本等防重复判断。
- 改为更直接的输出预留：正文开始前为兔子镜保留长度；篇幅不足时先收束正文，再精简内部次要文字与装饰，不得整段省略或留下未闭合结构。
- 末尾仅保留“正文后必须继续输出完整兔子镜”的强制契约，以降低兔子镜偶发缺失。
- 新增 class 型 `:checked` 局部急救：当 CSS 写成 `.trigger:checked ~ .panel`，但目标实际嵌套在同一 label 的后代容器中时，只在当前 label 内落实该状态，不会连带展开其他同类节点。
- 交互诊断新增 `CSS状态规则` 与 `cssChecked` 标记，便于确认该路线是否命中。
- 保留 0.32.39 的 Visual Scenery 自然交互、0.32.38 单条纯文字急救及其余运行逻辑。

## 0.32.39 Visual Scenery 自然交互去模板化

- 基于 0.32.38，保留单条一次性纯文字急救、叙事核心动态视觉规则与既有运行逻辑。
- Visual Scenery 仍要求每轮至少一个交互变化，不取消交互。
- 核心画面打开后即由持续动画完整成立，交互只负责呈现有意义的第二状态。
- 触发对象、操作方式与反馈结果必须随本轮内容重新设计，避免连续复用整幅点击、长按、按钮、开关或固定提示骨架。
- 不再强制使用整幅画面、透明热区或“长按/点击画面”提示；自然存在的灯光开关、门窗、物件、关系节点等仍可按本轮画面使用。
- 交互触发后必须改变内容显隐、关系结构、空间层级、构图、材质状态或观察方式，不能只换颜色、边框或阴影。

## 0.32.38 单条一次性纯文字急救

- 继续使用 0.32.37 的 0.32.33 稳定代码基线与叙事核心动态视觉规则。
- 将“纯文字急救”从全局复选框改为“修复单条纯文字兔子镜”按钮。
- 点击按钮后，再点击聊天中需要修复的那一条兔子镜；只处理该消息，不扫描其他历史消息。
- 急救使用临时消息副本重建当前 DOM，不修改 `chat[].mes`、当前 swipe 或 `extra.display_text`，也不调用聊天保存。
- 选择完成后自动退出；再次点击按钮、按 Esc 或等待 30 秒可取消。
- 代码块急救、智能交互急救、Prompt、Visual Scenery 与其他功能均保持 0.32.37 不变。

## 0.32.37 0.32.33基线回退・叙事核心动态视觉版

- 以 0.32.33 单镜输出锁版为完整代码基线，不包含 0.32.34～0.32.36 的 CSS 隔离与跨设备重新挂载逻辑。
- 替换 Visual Scenery 的【硬要求】：先寻找正文中的叙事核心，再据此生成完整、统一、持续变化的 CSS 动态视觉画面。
- 急救区显示顺序调整为：代码块急救 → 智能交互急救 → 纯文字急救，并按长期/临时使用场景精简说明。
- 三种急救的实际运行逻辑保持 0.32.33 不变。

- 修复部分模型把开头“硬启动锁”和末尾“最终输出契约”分别执行，导致正文上下各生成一份相同兔子镜的问题。
- 开头改为“启动锚点”：只预留长度，明确不得在正文前输出、预演、复制或占位。
- 末尾契约明确全回复只能出现一个 `<toto>...</toto>`，且必须位于正文之后、作为最后一个可见模块。
- 保留 0.32.32 的展现形式优先配色、全部交互急救、纯文字急救误伤保护与 Visual Scenery 规则。
- 未新增输出清洗或自动删除逻辑；本次仅修正 Prompt，避免误伤正常内容。

## 0.32.32 展现形式优先配色试验版

- 新增“展现形式优先配色”：先确定媒介本体、材质、时代、环境与光线，再自然推导配色。
- 不预设固定色板，也不恢复暗色比例、亮度指标或上一轮配色强制纠偏。
- 限制不同媒介被统一套成同一种科技界面、终端界面、玻璃面板或低照度模板。
- 高级感继续由构图、材质、空间、光影、细节与色彩关系建立；配色仍服从展现形式。
- 0.32.31 的硬启动输出锁、0.32.30 交互急救、纯文字健康 UI 保护与 Visual Scenery 规则均保留。

## 0.32.31 兔子镜硬启动锁修复

- 修复 `hardStartup` 设置长期存在但未参与 Prompt 构建的问题。
- 在注入最前部加入短启动锁，要求先预留兔子镜输出长度；在末尾保留唯一完整输出契约。
- 若输出长度冲突，要求缩短正文或兔子镜内部文案，而不是省略整个兔子镜。
- 0.32.30 的 querySelector 后代状态急救、0.32.29 的健康 UI 保护及全部既有功能保持不变。

## 0.32.30 querySelector 后代状态急救

- 智能交互急救现在可从原始消息安全解析 `this.querySelector('固定选择器').style.xxx='固定值'`。
- 目标先在原始兔子镜内定位，再映射到已渲染节点，兼容宿主给 class 自动添加 `custom-` 前缀。
- 同一原始 onclick 已被精确恢复时，不再叠加通用“遮罩揭示”路线，避免双重监听和提前展开。
- 只读取固定选择器与固定样式赋值，不执行模型生成的 JavaScript；目标不能越出当前兔子镜。
- 保留 0.32.29 的健康 UI 保护、纯文字急救隔离、Visual Scenery 规则及全部既有急救。

## 0.32.29 纯文字急救健康 UI 保护

- 修复纯文字急救在没有 CSS ERROR 时，仅因检测到 `var(...)` 就预防性改写正常兔子镜的问题。
- CSS 变量定义在主容器 inline style、引用在局部 `<style>` 时，会从整条兔子镜收集变量后再展开，不再误替换为 `initial`。
- 无法可靠解析的变量保持原样，绝不再用 `initial` 覆盖背景、文字色与强调色。
- 保留 0.32.28 的输出契约、Visual Scenery、全部交互急救与其他功能；Prompt 未修改。

## 0.32.28 延迟相邻元素交互急救

- 修复 onclick 先改变自身、再在 setTimeout 内修改 `this.nextElementSibling.style...` 时，只建立“元素自变化”路线却未揭示相邻内容的问题。
- 安全解析相邻/前置/父级元素的最终样式赋值，不执行模型生成的 JavaScript，并将目标限制在当前兔子镜内部。
- 保留 0.32.27 的输出契约、Visual Scenery 规则、纯文字急救及全部既有交互急救。

## 0.32.27 首轮输出契约后置修复

- 修复 0.32.26 删除末尾保底后，强制生成兔子镜的要求只出现在注入开头、可能被后续长规则稀释的问题。
- 保持“只出现一处”的设计，将完整输出契约移动到注入末尾；每轮正文结束后必须继续生成兔子镜。
- 不恢复重复规则，不修改 Visual Scenery、扫描、纯文字/代码块/交互急救与其他功能。

## 0.32.26 规则精简与动态画面本体

- 压缩“复杂交互视觉核心”，保留媒介本体、空间层级、视觉锚点、明显交互、骨架避让与触屏兼容，不降低视觉硬指标。
- 删除重复且效果有限的“UI 自查短版”及其设置项；实际输出扫描、近期视觉避让和风险纠偏继续保留。
- 移除未实际控制 Prompt 的 `forceInteractiveMode` 旧设置；内部交互仍由常驻复杂视觉核心保证。
- Visual Scenery 改为“完整动态插画场景”：主视觉或环境层持续运动，禁止静态画加少量粒子冒充动画。
- Visual Scenery 的交互必须融入整幅画、局部景物或透明热区，不得另加独立按钮、面板和说明区。
- 删除重复动画短句、重复最终保底和 UI 自查；将每轮必出兔子镜的要求合并进唯一强制输出协议。
- 放宽 `inline style`：普通静态局部可内联，动画、响应式结构和状态联动可使用局部 `<style>`。
- 缩短内部 details 冷却文案；急救库和 0.32.25 纯文字职责隔离保持不变。

## 0.32.25 纯文字急救职责归并
- 只调整纯文字急救与代码块急救的职责，不改 Prompt、Visual Scenery、交互急救或其他规则。
- 删除代码块急救中遗留的 `CSS ERROR` 检测与“原文无变化也强制重绘”入口。
- `CSS ERROR` 检测、CSS 变量展开、纯文字强制重绘与重复重绘保护，现在全部只受“纯文字急救”开关控制。
- 保留共用底层：`<style>` 保护与 `<br>` 清理、HTML/CSS 紧凑化、`:root` 局部作用域、消息即时重绘。
- 因此关闭纯文字急救后，代码块急救不会再仅因画面出现 `CSS ERROR` 而暗中重绘；真正的代码围栏、`<pre><code>`、裸 HTML 仍由代码块急救处理。

## 0.32.24 纯文字急救
- 新增独立“纯文字急救”开关：兔子镜出现 `CSS ERROR` 或退化成纯文字时，自动从原始消息回读并重绘。
- 将宿主旧 CSS 解析器不支持的 `--自定义属性` / `var(...)` 展开为直接值，再删除变量声明；支持中文变量名、嵌套引用与 fallback。
- 对常见的根 class 漏写点号进行保守修复：仅当 `oldcity {}` 可明确对应 `.rabbit-scenery-oldcity` 时才改写。
- 可与代码块急救、智能交互急救串联，顺序固定为：纯文字 → 代码块 → 交互。
- 保留 0.32.23 全部急救库与 Visual Scenery 可见动画规则；Prompt 文件未修改。

## 0.32.23 Visual Scenery 可见动画兑现修复

- 修复“源码里有 `animation/@keyframes`，实际看起来仍像静态图”的伪兑现。
- Visual Scenery 现在要求打开后 1 秒内，至少一个主视觉层出现肉眼可见的位移、旋转、缩放、遮罩或光影变化。
- 仅有 3～5px 微尘、同色弱光、低对比小点或极慢小幅漂移，一律不再算动画。
- 要求关键帧保留元素原有 `rotate/scale`，或改由外层承载，避免 `transform` 覆盖使画面退化。
- 保留 0.32.22 的全部交互急救、CSS ERROR 重绘、`:root` 外壳恢复与取消暗色审查基线。

## 0.32.22 直接三元状态提前退出修复

- 修复安全状态程序在没有 `const x = this.checked ? ...` 变量声明时过早返回的问题。
- 现在可正确接管 `this.nextElementSibling.style.opacity = this.checked ? '1' : '0'` 及同类 `transform`、括号属性、文字赋值。
- 仍只解析当前兔子镜内的有限 DOM 亲属链与安全样式，不执行模型生成的 JavaScript。
- 保留 0.32.21 全部急救库与 0.32.20 Visual Scenery 持续动画硬约束；Prompt 不变。

## 0.32.21 相邻元素三元状态急救

- 新增对 `this.nextElementSibling.style.xxx = this.checked ? ... : ...` 的安全回读与重建。
- 同时支持有限的 `previousElementSibling`、`parentElement` / `parentNode` 链及点号、方括号样式写法。
- 不执行模型输出的 JavaScript，目标始终限制在当前兔子镜内部。
- 保留 0.32.20 Visual Scenery 持续动画硬约束及 0.32.19 全部急救库。

## 0.32.20 Visual Scenery 持续动画硬约束

- 修复 Visual Scenery 仍受“动态可选”规则影响、模型可能只输出静态画面的冲突。
- Visual Scenery 每轮必须包含至少一组自动运行、持续可见的 `CSS animation + @keyframes`，并使用 `infinite`；仅 transition、hover、checkbox 切换、SVG animate 或 JS 均不能代替。
- 保留 0.32.19 全部交互急救、0.32.17 外壳变量修复，以及 0.32.18 取消暗色审查的基线。
- 通过压缩原有 Visual Scenery 文案抵消新增约束，相关 Prompt 总字符数不增加。

## 0.32.19 `getElementById` 安全状态程序急救

- 修复 `onchange` 被宿主删除后，`const x = document.getElementById('...')` + `if(this.checked){...}else{...}` 无法恢复的问题。
- 仅在当前兔子镜内部解析固定 ID 与样式赋值，不执行模型 JavaScript，也不访问整页 DOM。
- 覆盖多目标同步变化：`transform`、`background`、`boxShadow`、`opacity` 等可随 checkbox/radio 切换并可逆恢复。
- 保留 0.32.18 的“取消暗色/配色审查”基线与 0.32.17+ 全部急救库；Prompt 文件未改。

## 0.32.18 取消暗色/配色审查，保留 0.32.17 急救库

- 取消读取上一轮实际颜色并注入“明度、综合色相、冷暖、饱和度必须变化”的配色节奏纠偏。
- 取消暗色主底、浅色主底与综合色族的自动历史判定，不再因为上一轮颜色强制本轮改用另一类底色。
- 删除“严禁单一套用黑底发光/紫蓝渐变”等暗色定向审查句；UI 自查与结构去模板化仍保留。
- 视觉扫描器不再采集或写入 paletteFingerprint，只保留结构骨架、媒介本体与交互风险检查。
- 完整保留 0.32.17 急救库：三类交互补全、可点击后置内容、CSS ERROR 即时重绘、`:root` 外壳变量恢复。
- 未增加 Prompt；相较原包反而减少配色纠偏注入。

## 0.32.17 `:root` 变量外壳恢复

- 修复完整 CSS 已恢复、但 `:root` 自定义变量在酒馆消息 CSS 隔离后失效，导致背景、卡带外壳、标签纸、进度条、文字配色与宽度消失的问题。
- 将消息内 `:root` 改写为当前兔子镜专属的稳定局部作用域，并同时标记 `<toto>` 与首个真实内容根；宿主移除未知 `<toto>` 时仍能继承变量。
- 每条作品使用独立作用域，不会让一条兔子镜的变量污染其他消息。
- 保留 0.32.16 的可点击后置内容急救与 0.32.15 的 CSS ERROR 即时重绘；`src/promptBuilder.js` 与全部 Prompt 数据未修改。

## 0.32.16 可点击后置隐藏内容急救

- 修复 `cursor:pointer` 的普通 `div/span` 后紧邻 `display:none` 正文时，因不是 `<button>`、也没有弹层关闭按钮而完全不命中急救的问题。
- 新增“可点击后置内容”独立路线：点击一次显示，第二次恢复；支持触屏点击与键盘 Enter/Space。
- 该路线只接管带明确“点击/确认/查看/封存”等提示的可点击容器，并排除绝对定位弹层和已有关闭按钮结构，避免与现有弹层路线重复。
- 一次性诊断新增“可点击后置内容”统计。
- 保留 0.32.15 的 CSS ERROR 即时重绘修复；`src/promptBuilder.js` 与全部 Prompt 数据未修改。

## 0.32.15 CSS ERROR 即时重绘急救

- 修复完整 `<style>` 首次渲染时被酒馆 Showdown 换行处理后出现 `CSS ERROR: ... missing '}'`，只剩纯文字的问题。
- 原始消息压缩完成后，调用酒馆原生 `updateMessageBlock` 立即重绘当前消息，不再依赖刷新页面或重新进入聊天。
- `<style>` 保护槽额外清除 `<br>` / `<br/>` / `<br />`，兼容不同换行写法。
- 保留已展开的 `<details>` 状态；不新增 Prompt，不改三类交互急救。

## 0.32.14 三类交互急救补全

- 新增“元素自变化”急救：宿主删除 `onclick` 后，仍可恢复 `this.innerHTML / innerText / textContent` 与 `this.style.*` 的安全固定值变化；普通交互保持可逆。
- 新增 checkbox/radio 的三元状态程序急救：支持 `this.checked ? A : B`、多层 `parentNode/parentElement + querySelector`、被净化器改成 `custom-` 前缀的 class，以及受限的 `style.cssText` 固定声明。
- 新增“可点击容器内部揭示”急救：同一容器内的点击提示与隐藏正文可独立展开、再次点击恢复；多个容器互不串联。
- 加强 label 点击状态确认，避免部分移动 WebView 在急救后又把 checkbox 切回原状态。
- 一次性诊断新增“元素自变化 / 安全状态程序 / 容器内揭示”路线统计。
- `src/promptBuilder.js`、主题库、展现形式库与全部 Prompt 数据未修改。

## 0.32.12 checkbox / radio → ID目标显隐急救

- 新增安全解析：`document.getElementById('id').style.xxx = this.checked ? 'A' : 'B'`。
- 动作绑定到 `input/change`，兼容隐藏 input 的 label 手动切换，不依赖原始 `click` 事件。
- 每个控件只操作当前兔子镜内对应 ID，多个控件与多个目标可独立开关。
- 交互完全可逆；不执行模型生成的 JavaScript。
- 一次性诊断新增“ID目标显隐”命中信息。
- Prompt 相关文件未修改。

## 0.32.11 可点击画面—相邻弹层可逆急救

- 新增“带 cursor:pointer 的画面 + 紧邻隐藏弹层 + 弹层内关闭按钮”结构识别。
- 点击画面打开弹层，点击关闭入口恢复，可反复打开/关闭。
- 不依赖已被宿主删除的 onclick；优先于普通 hover/pseudo 兜底。
- 一次性诊断新增“可点击画面弹层”命中项。
- Prompt 相关文件未修改。

## 0.32.10 按钮后置隐藏内容可逆急救

- 新增普通 `button` 后紧邻隐藏内容的渲染后急救。
- 点击一次显示，第二次点击精确恢复；不会照搬模型脚本把按钮永久隐藏。
- 此路线优先于触屏 hover 兜底，避免只改变按钮颜色却不显示内容。
- 一次性诊断新增“按钮后置内容”命中信息。
- Prompt 注入文件与 0.32.9 保持一致。

## 0.32.9 可逆交互＋Label 内弹层急救

- 新增统一可逆状态基线：第一次接管时保存原始样式与文字，取消交互时精确恢复。
- 修复宿主克隆或重建当前 DOM 后，监听器丢失、交互停留在打开状态的问题；新 DOM 会重新绑定，但不会把打开后的样式误记成初始状态。
- 同一个 checkbox/radio 只允许一条渲染后结构急救路线接管，避免多个兜底重复写样式。
- 新增 label 内“触发文字隔在中间＋隐藏绝对定位弹层”识别；显示时临时转为不被 overflow 裁切的流式内容，第二次点击恢复原弹层样式。
- label 后置结果层与相邻隐藏内容可点击结果区返回；遮罩揭示也支持 DOM 重建后继续开关。
- 一次性诊断继续保留，并显示当前结构型急救路线。
- Prompt、配色节奏、内部折叠冷却与 Visual Scenery 规则均未修改。

## 0.32.8 遮罩揭示＋后置结果层急救

- 新增无表单控件的“遮罩层＋隐藏正文层”点击/轻触揭示急救，覆盖被宿主删除 `onmouseover/onmouseout` 的结构。
- 新增 `label + checkbox/radio` 后方紧邻单块结果层的显示急救，并可增强同画布中的零尺寸圆形视觉主体。
- 设置页按钮强制横向排版，手机端不再逐字竖排。
- 一次性诊断继续保留，并新增上述两条急救路线的命中信息。
- Prompt 规则未修改。

# RabbitMirror 0.31.92

本版以 0.31.91 为底座，仅增强智能交互急救。

- 修复宿主在 DOM 渲染前删除 `onclick` 后，0.31.91 无法再读取伪交互程序的问题。
- 急救器会从当前聊天消息的原始 HTML 中回读安全可解析的 `document.getElementById(...)` 样式与文字赋值。
- 通过兔子镜标题与元素子路径映射回当前渲染节点，目标仍严格限制在当前兔子镜内部。
- 不执行原 JavaScript，不使用 `eval`；包含未知语句时仍然拒绝。
- Prompt、Visual Scenery、主题库、设置键、代码块急救均未修改。

详细变更见 `README-0.31.92.md`。

## 0.32.5 正式修复版
- 移除 0.32.4 的临时可视化诊断面板。
- 新增 label 内单块隐藏内容急救：排除隐藏 input，识别同一 label 中真正的隐藏正文。
- 勾选时同步修复 opacity、visibility、transform、display、height、max-height 与 overflow 裁切；取消后恢复原样。
- 兼容宿主删除 onchange 的情况，不执行模型生成的 JavaScript。
- Prompt 与 0.32.4 保持一致，不增加模型输入规则。

## 0.32.7 正式内置诊断版
- 保留 0.32.5 的 label 内隐藏内容急救。
- 新增默认关闭的“交互诊断面板（反馈用）”开关。
- 开启后在每个兔子镜底部显示版本、命中路线、事件、控件状态、隐藏候选、实际样式与裁切信息，并可一键复制。
- 关闭开关后移除诊断面板与诊断事件监听。
- 诊断只在浏览器本地运行，正文仅显示截断片段，不上传数据。
- Prompt、配色节奏、折叠冷却与 Visual Scenery 规则均未修改。


## 0.32.7 一次性轻量诊断
- 将常驻“交互诊断面板”改为“开始一次交互诊断”按钮。
- 只监听聊天区下一次被点击的兔子镜；捕获约 650ms 后自动停止全部诊断监听。
- 报告保留在当前兔子镜底部，可复制“诊断＋原始源码＋实际渲染代码”，也可关闭或重新诊断。
- 仅在点击复制时读取源码；超长 data URI / Base64 自动省略。
- 不修改 Prompt，新增诊断逻辑不增加模型输入 token。
