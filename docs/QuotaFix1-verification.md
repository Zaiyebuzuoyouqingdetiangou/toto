# 兔子镜小剧场 1.5.50 / QuotaFix1 验证说明

- 唯一代码基线：用户上传 `toto-main 5.zip`，内部版本 1.5.48。
- 原包 SHA-256：`17f8c474f957e0d6a1625cb79bf1347e462e3a511cc3e5b9f88bcfd8b836fd23`。
- 候选版本：`1.5.50`；运行时／ESM 缓存标记：`1.5.50-quota1`。
- 原 ZIP 保留；未读取或写入 GitHub，没有提交、推送、分支或 PR。

## 修复内容

1. `src/storage.js` 的新批次登记改为紧凑编码保存完整冻结计划；`src/batchPlanCodec.js` 无损恢复原内容。标题、摘要、规则原文、自定义字段、外部 ID、UI／交互要求及属性顺序均保留；未按当前母本重新抽签或拼凑恢复。
2. 编码只有在完整保存值的 UTF-16 字符数与 UTF-8 字节数均减少时采用，否则保存原文。字典、输入及解码输出均有界；不依赖网络或第三方库。仅在批次保存／读取时运行，未增加启动解压、轮询或 DOM 扫描。
3. 新批次使用 v3 登记键；旧 v2 登记继续可读取、提交和释放，原记录不在读取时迁移或清空。消息、Swipe、正文指纹、批次冲突比较和单次付费请求凭证沿用原逻辑。
4. 新成功冷却历史只去掉 `themes`、`formats` 两个完整材料数组；保留所有其他组合、UI、交互、视觉、冷却及面状态字段。已存成品、维修结果、母本及历史记录不批量删除或改写。
5. 成功提交先执行缩小的写入，再增加历史，避免最终容量够用却因瞬时需要同时放下登记与历史而失败。失败仍按原所有权检查回滚；容量不足不会绕过登记发送。

## 验证结果

整合目录及最终 ZIP 重新解压后均执行同一验证入口：

```bash
RABBIT_MIRROR_BASELINE=/绝对路径/原包/toto-main \
RM_BASELINE_STORAGE_SOURCE=/绝对路径/原包/toto-main/src/storage.js \
python tools/verify_quota_fix.py
```

环境：Node v24.19.0、Python 3；VM 模块用于替代缺失的酒馆宿主边界，不安装依赖、不进行真实模型请求。

- 定向回归 **40/40 通过，0 失败、0 跳过**：编码 9 项、存储 16 项、Prompt／发送保护集成 15 项。
- JS/MJS 语法 **89/89 通过**；本地字面量导入目标 **185 条通过**；不存在旧 `1.5.48-release1` 入站缓存引用。外部酒馆宿主模块单独标记，不冒充本地缺失文件。
- 五面计划保存后，在无原内存缓存的新运行环境中精确恢复；包含内置、外部、自定义、长 ID、中文、日文、emoji 及 UI／交互字段。
- 跟随模式、独立模式：完整 Prompt、近输出执行锁、元数据与原包逐字一致；成功历史精简后，下一轮实际抽取及 Prompt 亦与原包一致。
- fetch／宿主服务两条边界：合法任务一次发送；重复凭证、取消、正文边界缺失、配额不足均不追加付费请求。存储幂等、消息归属、部分成功、旧登记共存、损坏记录拒绝及回滚均有定向覆盖。

容量对照采用相同内置条目与模拟存储，以 UTF-16 code units（含键名）计量。例如一次完整测试：旧版两面 4,810、旧版五面 9,050、候选五面 7,335；同一容量 7,367 时，旧两面成功、旧五面报 `BATCH_STORAGE_QUOTA_EXCEEDED`、候选五面成功。批次 ID 随运行变化，数字会略有浮动；这些不是用户手机的实际容量统计。

仅 `src/storage.js` 有现有业务逻辑改动，另新增编码模块；其余现有 JS 仅更新运行版本／缓存标记。Prompt、原始规则库、随机算法、响应解析、HTML 安全净化、交互、CSS、维修兔与挨打猫的业务代码没有改动。逐文件 SHA-256 清单位于 `docs/QuotaFix1-file-sha256.json`（清单自身除外）。

## 验证边界

**仅完成源码／自动化验证，尚未真机验证。** 未测试真实 SillyTavern／TT／iPhone 操作、真实网络及真实模型；没有完整模拟嵌套生成、漏结束事件、SSE／NDJSON 等宿主生命周期。本轮这些路径未作业务修改，不能把存储和发送边界的测试当作其真机验收。

原包不含 tests、package.json 或可执行的历史全量测试，无法重跑此前版本报告里的旧测试数字；40 项是本次实际执行的新定向测试。离线重新执行基线对照，需要原 ZIP 解压目录；未提供时相关基线对比项会明确跳过。

仍没有用户站点的实际存储占用统计。如果连紧凑记录也没有容量容纳，仍会在发送前停止；不删除其他插件数据，不清空站点、不增加付费重试。原有跨页签 localStorage 事务不是数据库原子事务，本轮没有宣称消除该既有平台限制。

## 安装与确认

将候选 ZIP 内 `toto-main/` 的内容覆盖到原兔子镜扩展目录，保留原包备份；不要再套一层文件夹，不要并行启用第二份插件。完整刷新／重启后确认 **1.5.50**，保持原设置，在原失败场景选择五面，手动重新生成一次。无需清缓存或重新导入母本。

用户真机确认前，本包为独立候选，不晋升正式版，不上传正式仓库。
